# 一致性768收尾：冻结状态的同预算读出校准

2026-09-25，用户确认后实施。研究目标仍是可复用的因果bar历史表示；历史方向、波动和当前字段只是固定探针，不将训练编码器拟合规则作为目标。

## 唯一问题与固定边界

上一轮[一致性复核](BABEL_CONSISTENCY768_REVIEW.md)确认共同历史差异下降，但旧冻结用途读出兼容性未保住。本轮仅检验：重新校准相同复杂度的线性读出，能否保留新状态的历史用途。编码器/解码器更新0，不改变重建损失、PCA基底、归一化或训练数据，不增加MLP、不扫一致性权重。

固定六份状态：原path768/control_s42/43旧best（总300/298轮），本轮consistency768/control_s42/43和consistent_s42/43的原选best（均总400轮）。新来源必须完成四组预算，且选中best就是last epoch；不允许现场挑其它epoch、只挑seed43或改用验证用途选编码器。旧best状态直接复用已验证utility768_v2缓存；四份新状态从锁定权重冻结提取，再通过旧读出逐预测回放上一轮报告，验证端点/状态/目标一致。

使用原4789训练、978验证、984原品种研究、453跨品种研究窗口，不用36106稠密池。严格对齐原始128×28标准化输入、当前28字段、13目标和可用mask，以及coverage交易日所属周；保留原合约边界与分区审计。共同历史稳定性来自上一轮已完成报告，本次不重复神经训练或重叠推理。

## 拟合公平与锁定

六份状态各13目标、原五档Ridge正则，每目标仅按原验证NMSE选择，训练mask及训练统计独立使用，平局按原实现偏好较强正则。合计390个闭式候选、78个选中头，FP64线性求解，0神经优化器更新。旧best也重新拟合，不只让新模型有校准机会；其参数选择/验证预测及研究分数必须回放原读出，避免数值或协议变化制造收益。

输入PCA768、完整3584维输入、当前28维输入复用原utility768_v2精确锁定的PCA和Ridge头。它们的输入、目标、训练/验证、算法、五档选参机会均未变，重复拟合不能增加比较信息；不增加它们或新状态的选参机会。全部基线评分回放原报告，训练均值一并输出。六份状态的所有新头落盘并锁定后才提取新研究embedding/评价；不利用研究结果重选alpha。

已完成来源含研究成绩，本实验本身也是依据研究结果提出，因此这些集合仍是多次复用的研究集，绝不称新独立留出。

## 一次输出与事前决定

同一次运行输出六份状态的新读出与原seed对应旧读出、三个原输入基线、训练均值。13目标全部报告，按品种/周期分组，附预测/真值/mask、逐窗口误差和交易周配对bootstrap区间。主要用途是16/64方向效率及log波动四目标，两项趋势直线拟合度只作诊断；当前七字段独立判断。

**校准恢复条件**：两种子、两个研究集全部满足。候选新读出相对同预算control新读出、旧best重新拟合读出，四目标综合NMSE最多增加5%，方向与波动族分别最多增加10%；周配对区间上界≤0，至少50窗口/5周，比较双方四目标R²均≥0.5。候选当前七字段各R²≥0.8，训练方差标准化NMSE周区间上界≤0.1，缺失字段不计为0误差支持。相同周bootstrap1000次、seed42，未作多重比较校正。

另外并列重算原utility768_v2相对raw/PCA/current的完整协议，作为独立的绝对用途结论，**不因为校准恢复就声称超过完整输入或原用途验收通过**。

- 所有恢复条件通过：`readout_recovered_stability_candidate`。仅说明需要配套新读出的一致性候选恢复了相当的线性可读性，保留候选供阶段决定，无自动部署或替换。
- 任一恢复条件失败：`stop_consistency010`，结束当前0.10配置，不自动扩网格、加训或改架构抢救分数。仍不能声称所有一致性方法或信息论意义的保留都失败。
- 原冻结接口结果始终保持`no_full_upgrade`。重新校准成功也不意味着可以直接沿用旧头，不将历史实验判据事后改写。

## 执行、恢复和导出

要求保留完整`checkpoints/babel_consistency768`、`babel_path768`、`babel_utility768_v2`及原来源数据和依赖。原Torch2.8.0+cu128/NumPy2.3.2环境。默认batch128与原报告提取批次一致，仅影响冻结提取，不是训练batch。

```bash
cd /root/autodl-tmp/obson
git switch features/babel
git pull --ff-only origin features/babel
mkdir -p logs /root/autodl-tmp/download
nohup bash scripts/babel_consistency_readout768_autodl.sh all \
  > logs/babel_consistency_readout768.log 2>&1 &
tail -f logs/babel_consistency_readout768.log
```

无100轮训练。预计数分钟至十几分钟，来源大文件哈希、磁盘和原验证回放可能占主要时间；以日志实测为准，不以GPU显存是否占满判断进度。旧/新模型及全部对照在一次命令完成，不另开多个竞争GPU的运行。

成功或失败自动导出：`/root/autodl-tmp/download/babel_consistency_readout768_reports.tar.gz`。包含来源/权重哈希、完整拟合候选与选择、数据身份、验证因果检查、旧读出回放、新旧预测及分组/区间/决策、日志和状态，不包含.pt/.npy/.npz。发回此一个包即可，无需额外导出权重。

手动重新打包：

```bash
cd /root/autodl-tmp/obson
bash scripts/babel_consistency_readout768_autodl.sh export
```

支持BABEL_RECAL_SOURCE/RUN/LOG/BATCH、BABEL_DOWNLOAD_DIR和PYTHON_BIN。改配置需新输出目录；同配置中断时已签名提取缓存可复用，未锁定的部分拟合整批重算，研究评分可重算。完整完成后再次执行只检查，不重复拟合。失败日志保留，最终command_exit_code和run_status明确区分。旧hash绑定源码不修改；本地只用合成数据测试，不运行真实模型训练、推理或读出拟合。

## 提交前复核

25项相关合成/回归测试通过，含8项新增：六份状态390候选的完整校准与新旧读出评价、可逆坐标变化恢复、真实用途退化拒绝、缺失支持拒绝、输入/真值/掩码/顺序校验、冻结提取不更新与缓存篡改拒绝、全部头锁定前禁止研究提取、部分拟合中断恢复、完整运行免重复拟合、祖先目录保护及失败导出。神经优化器没有实际step；闭式拟合仅使用小型合成数组。

按来源→权重→数据→拟合→选模→评价逐项自查，同时逆向确认任何用途失败不能被稳定性收益覆盖。发现旧目录保护函数只接受bar-alignment层级，已为本轮新来源实现显式祖先保护，避免新manifest被当作旧层级解读；用用户上传的真实consistency768清单确认默认输出目录可用，全程不加载真实权重。上一轮44项绑定Python源码哈希完全不变。此为本地自查与合成验证，正式CUDA提取/拟合和结论仍待AutoDL。
