# 一致性768读出收尾：校准恢复，保留稳定性候选

2026-09-25，收到 `babel_consistency_readout768_reports.tar.gz`。本轮事前声明的恢复检查 **52/52通过**，结果为 `readout_recovered_stability_candidate`。可以保留一致性768及其新读出器作为稳定性研究候选，结束这条收尾实验支线。原完整用途协议仍失败；旧冻结接口的 `no_full_upgrade` 不改写，不自动替换已交付模型。

## 完成情况与核验

六份状态全程冻结，0编码器/神经读出更新，0新PCA拟合；6状态×13目标×5原正则=390个闭式候选、78个选中头。原4789训练、978验证、984原品种研究、453跨品种研究窗口，训练/验证样本及交易日所属周与旧读出清单完全一致，未使用稠密候选池重新拟合。runtime记录101.49秒，不含初始来源检查，正式执行退出码0。

本地复核20项报告文件、45项绑定源码、55项可用上游文件哈希；78项选参均为各自原五档候选中验证NMSE最小者，390候选预算匹配。7204行清单与原utility768_v2一致，四份新权重的原验证回放及140项因果检查通过；旧best两份缓存和原拟合结果保持来源绑定。

用导出的预测/真值/mask独立重算298896个预测值对应的用途误差、5408项总体/分组目标分数、2220项交易周配对区间，以及52项恢复判断和48项原用途判断，均匹配。12份旧读出逐预测回放、6份输入基线分数回放一致。旧best重新拟合的选参和结果匹配原读出，因此没有借换拟合协议或更宽网格制造恢复。

包中省略5份数组文件，上游8份权重未附带；哈希身份可核验，实际CUDA权重、缓存及拟合不在本地重新执行。本地仅报告算术/元数据复核，不是独立同行评审或独立新数据验证。

包SHA256：`59b710f9da2cd3ee30da3d4e8d952612ad2f2d5bc665a9382e333064068bb659`。

## 校准后恢复了什么

主用途NMSE越低越好，四目标为16/64历史方向效率与log波动；当前字段与两项趋势直线拟合度不混入此数。新旧编码器都冻结，只重新拟合相同复杂度的线性头。

| 研究集 / 种子 | 旧best重新拟合 | 普通续训重新拟合 | 一致性＋旧头 | 一致性＋新头 | 新头结果相对普通续训 |
|---|---:|---:|---:|---:|---:|
| 原品种 / 42 | 0.170539 | 0.170714 | 0.205953 | 0.172336 | +0.95% |
| 原品种 / 43 | 0.150096 | 0.150211 | 0.158641 | 0.150220 | +0.01% |
| 跨品种 / 42 | 0.057598 | 0.057066 | 0.090039 | 0.056525 | -0.95% |
| 跨品种 / 43 | 0.059770 | 0.059468 | 0.066644 | 0.059041 | -0.72% |

一致性候选相对旧best的对应变化为+1.05%、+0.08%、-1.86%、-1.22%。两个种子、两个研究集均通过综合误差最多5%、方向/波动任务族各最多10%的周区间保留条件；并非仅靠四组均值过关，也未重新选择编码器epoch或种子。当前七字段的绝对保留检查全部通过，候选最小当前字段R²约0.976。

种子42旧接口的明显代价在校准后恢复：原品种用途误差从0.205953降至0.172336，跨品种从0.090039降至0.056525。结合普通续训旧头也曾退化，证据支持本次原四项用途的主要问题是读出接口不适配，而非发生无法由相同复杂度线性头恢复的明显损失。不能进一步声称embedding只是做了可逆旋转、全部历史信息都未损失，或所有可能用途均保留。

上一轮同一批一致性权重已确认：训练位移1/16的共同历史回读差异相对同预算control下降17.77%–23.79%，未训练位移64下降10.85%–11.94%；全局综合误差代价仅0.135%–0.656%，原重建保护通过。**本轮没有再次训练编码器，新增证据是这份更稳定状态的配套读出可恢复原用途。** 不把稳定性与校准两个报告写成两次模型训练突破。

## 原用途门槛为什么仍失败

| 四目标用途NMSE，两种子均值 | 原品种 | 跨品种 |
|---|---:|---:|
| 一致性768＋新头 | 0.161278 | 0.057783 |
| 完整输入3584维 | 0.148786 | 0.051396 |
| 输入PCA768 | 0.160776 | 0.096553 |
| 当前28特征 | 0.321737 | 0.204209 |

候选相对完整输入仍高8.40%/12.43%，原品种与输入PCA均值接近，跨品种比输入PCA低40.15%。这些均值是两个种子指标平均，不是集成。原协议20个历史用途判断仅6个通过，28个当前字段判断全过；四个候选×研究集单元均utility_passed=false。因此“校准后保留已有能力”成立，“相对所有信息基线形成完整用途优势”仍不成立。

方向描述可用作有限研究功能：两种子均值，16/64历史方向三分类平衡准确率为原品种86.32%/90.23%、跨品种90.20%/88.90%。它描述已观察历史的规则标签，不是下一根方向准确率，也不是可直接开仓的证据。趋势直线拟合度仍弱：原品种64根R²约0.0035/-0.0796，不作为可靠趋势强度输出。

## 阶段决定与交付边界

1. 保留consistency768/consistent_s42和consistent_s43总400轮原best，分别绑定本轮heads中的同名新读出器及目标/输入缩放参数。两份都保留，不因某个指标挑单一种子；原control768旧best及其读出继续作为冻结参照，512交付基线不被自动替换。
2. 结束本条一致性训练＋校准支线，不追加encoder轮数、不搜索辅助权重或更大读出。该固定配置取得了“共同历史更稳定、主要线性用途保留”的有限进展；这是本次可证伪问题的阶段终点。
3. 下一步应把固定版本用于明确的状态接口交付：每根已收盘bar的768向量、历史回读、16/64方向与波动描述、当前字段及来源信息。必须配套新读出并单独核验接口，不能让旧头读取新向量；这属于将既有成果做成可调用版本，不应包装为另一轮研究突破。该新接口尚未实现/验证，本次没有发起新训练。
4. 通用表示、趋势强度、无限历史和未来预测仍未完成。当前结果来自反复使用的研究集，独立泛化确认另行安排，不以临时补几天数据为现在工作的前置条件。

本次只更新结果与台账，未改任何训练/模型/评价源码或原门槛。
