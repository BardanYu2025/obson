# Babel 主线阶段判断：压缩原型成立，通用表示尚未成立

解释澄清与一次冻结对照已准备（2026-09-26）：此前不同解码器、状态位置及历史片段的成绩不能支持“128根能重建而80根不能”的说法。新增[原解码器同目标对照](BABEL_DECODER_PARITY768.md)：control600两种子冻结，同一状态用原全局头与原局部头读取相同过去16根；另固定结束时刻/目标比较32/64/80/128直接上下文。原全局中途槽位语义明确作为待检验假设，不冒充已成立的统一监督。0训练/新头/拟合，35项合成及回归检查通过、原65份源码哈希不变，真实效果待AutoDL。一次完成即收尾，不自动加训；跨分辨率共同宏观表示仍是后续核心设计问题，完整用途未通过的结论不改。

History Query768正式复核（2026-09-26）：[结果与阶段收束](BABEL_HISTORY_QUERY768_REVIEW.md)确认运行5小时51分、700条阶段epoch记录预算/采样/选模和504项判据复算一致，81个报告及65份源码哈希通过。joint相对同预算control的新查询中途原生误差改善6.28%～7.77%，但新头按相同最近16根目标评分仍比自身原局部头差85.55%～227.46%；原末端近期代价9.20%～14.39%，新查询共同历史差异扩大13.07%～42.14%，用途增量跨种子不一致。正式no_joint_query_gain保留，停止本配置及默认追加逐点重建优化，不因loss仍下降自动续训；保留control600接口和本轮control700等研究权重，不自动晋升。跨分辨率共同宏观表示尚未由本轮检验，下一核心问题须对齐物理区间、区分共享内容与尺度细节，并以表示用途事前定义退出条件；尚未新增训练。


统一历史查询实验已准备（2026-09-26）：用户批准核心结构探索后，实现[History Query768](BABEL_HISTORY_QUERY768.md)。在已验证control600上增加仅接收单个bar向量与相对历史距离的共享cross-attention解码器，统一读回可见短/长历史；使用当前已收盘价定义目标坐标、距离平方根尺度，不给解码器行情或真实锚点捷径。每种子共同50轮查询头预热，然后frozen/control/joint三组各100轮；新头预算相同，control/joint原训练预算相同，joint仅增加回到编码器的0.25查询梯度。原目标/同周期一致性及完整用途保护保留，全部六组选模及780个用途候选选择锁定后才评价两份重复研究集；固定矩阵完成后停止，不自动晋升或扩网格。真实CUDA运行待AutoDL，本地无真实训练/推理。


control600固定研究接口通过（2026-09-26）：[正式报告复核](BABEL_CONTROL600_REVIEW.md)确认v2运行263.52秒、退出0、零更新/零拟合，原两种子两研究集回放全部通过，72个固定滚动状态对独立参考最大差异0。25份可用产物、62份交付源码/59份运行源码、来源及凭证链核验通过；29576个源评分/样例数值复核，8份导出状态的锚点/时间/读出正常，离线图与原报告重建完全一致。本地无权重、未重新运行真实推理。本轮工程收尾，不再要求重复训练/打包；保留control600＋原局部头＋原校准读出的研究接口，512交付基线与完整用途未通过结论不变。下一阶段完整论证统一bar中心历史表达/共享解码及联合训练，允许较大改动但须有公平对照，尚未启动新训练矩阵。


control600打包修复（2026-09-26）：首次远端打包暴露实际输入结构恢复遗漏：继承的PathInput即使关闭也保留包装器与buffer，普通Linear无法strict load。现显式记录/恢复输入适配层，保留严格加载及全部权重签名；补足关闭/打开包装器的四层完整打包测试和真实768/4层/8头/FFN3072规格的合成前向测试，共17项相关检查通过。57份既有绑定代码不改。新输出control600_v2保留失败记录，仍是零训练打包，真实权重回放待重跑。


固定研究接口已准备（2026-09-26）：用户要求先整理成果，并允许下一核心改动更大但须完整论证。[control600接口](BABEL_CONTROL600.md)固定两个种子的原600轮模型、原局部头与已有校准读出；零训练/零拟合，打包后严格回放全部原研究误差和读出，并检查72个固定滚动状态，成功才签发模型包。新增离线图按原等距样本展示完整/近期路径、单步变化和成交持仓活动，本地已有真实报告可视化可直接查看；真实权重打包与回放待AutoDL。14项相关合成测试通过；旧57份绑定源码不改，512交付基线及完整用途失败结论不改。[下一核心改动论证](BABEL_NEXT_CORE_REASONING.md)优先审视末端PCA坐标与中途局部监督不统一的问题，保留更大结构调整空间；尚未锁定新训练实验，不再自动续训局部头或恢复旧跨周期权重。


原局部头warm-start正式结果（2026-09-26）：[完整复核](BABEL_LOCAL_WARMSTART768_REVIEW.md)确认两种子各100轮/3800步完成，原函数与研究回放通过；正式no_warm_local_readout_gain。训练位置组误差下降2.55%～4.29%，其绝对改善91.56%～98.05%来自p128；留出48/80/112仅−0.70%～+0.055%，未达到预定5%。104项中96项保留全过、8个增益项未过，不能写成模型准确率或全面退化。保留control600及原头为默认研究组合，新头仅作小幅微调记录；结束本轮局部头配置，不自动扩头/续训/调LR。原完整用途未通过，通用bar表示尚未成立。跨周期方向保留，不因本轮结果恢复已否决的0.10约束。下一架构实验需先明确整体/中途信息分配假设，本次仅归档，不新增训练任务。


原局部头warm-start已准备（2026-09-26）：用户同意回到中途bar回读主线后，实现[BABEL_LOCAL_WARMSTART768.md](BABEL_LOCAL_WARMSTART768.md)。仅control600两个种子，各自原头精确恢复、raw state恒等处理、编码器/全局解码器/用途头冻结；各100轮、3800次头更新、1915600位置曝光，固定峰值3e-5，不再随机头或拟合新状态标准化。原头是epoch0回退，原验证32/64/96/128选模，研究48/80/112检查至少5%改善并保护逐位置及分项，两种子/两个集合/best+last同时满足才记有限局部增益。33项相关合成检查通过，56份旧绑定模块不改；来源真实清单元数据兼容，未本地真实推理/训练，正式结果待AutoDL。停止自动扩网格，跨周期方向保留而非彻底放弃；既有完整用途与交付结论不变。


跨周期正式结果（2026-09-26）：[完整复核](BABEL_CROSS_PERIOD768_REVIEW.md)确认四组各100轮完成、best=last，总600轮；补评价严格复现父batch128，原batch64评分保持不变，55份旧绑定代码不改。15→30共同变化差异下降15.42%～17.91%，但15→60细周期真实误差增加6.55%～11.11%；全局+5.06%～11.61%、近期+10.76%～21.23%、活动+12.31%～15.19%（均对同预算control）。648项重算一致，正式no_cross_period_upgrade，不通过比例不等于准确率。本轮0.10跨周期约束停止、不扩权重网格；control600相对父500全局均值改善15.98%/26.10%，保留更强重建研究候选及已校准读出，但完整用途仍未过，不替换交付版本。收益同时伴随追加预算与三视图监督，不能单因归因。后续回到原局部头warm-start的中途bar回读问题，尚未实现新实验；此次只复核归档，不要求再训练。


2026-09-24。用户指出实验越来越难理解，并要求判断是否有突破。本文件收束已有证据，覆盖旧文档中继续展示、扩读出等下一步建议；它不修改历史实验的原始判据，也不宣告某一类模型无效。

最新决定（2026-09-25）：用户已明确不以补数据阻塞研究，撤回下文将新时间段列为前置条件的安排。[768显式路径输入与配对续训已完成](BABEL_PATH768_REVIEW.md)：主要综合收益来自追加训练，新增输入只有小幅修正，完整升级未通过。现有768续训模型保留综合优势研究候选，512仍是既有交付基线；不再为这一输入分支自动扩网格或继续加训。新分解表明部分失败集中于重建曲线的整体位置/长程漂移，后续结构问题应围绕同一状态如何保留这些信息，而非盲目加维度。尚无新的解码架构实现或运行。最终独立确认延后。下文保留该决定形成前的证据与历史安排，当前行动以本段为准。

用途验收准备（2026-09-25）：[冻结768统一用途协议](BABEL_UTILITY768.md)已锁定并实现。原输入续训control为主要候选，path为次要对照，四份原best冻结，统一对比输入PCA768、完整输入和当前特征。历史方向/波动与当前bar保留分别判定；不再训练编码器，不扩读出网格。正式运行与结果仍待AutoDL，不预写通过或主线升级。

用途验收结果（2026-09-25）：[768冻结复核](BABEL_UTILITY768_REVIEW.md)确认当前bar七项字段保留通过、方向/波动可读，但完整用途门槛未过，control/path均不晋升。主要候选相对raw综合代价7.75%/14.18%，跨品种优于输入PCA，原品种无统一增量证据。结束本次读出搜索；可针对用户新问题做冻结重叠窗口诊断，尚未实现，不自动加一致性训练或更改主模型。

重叠诊断准备（2026-09-25）：[单次冻结诊断](BABEL_OVERLAP768.md)已实现，固定两份control768检查点，1/16/64错位共同历史比较；没有新训练、拟合或用途门槛修改。只判断现有回读的上下文稳定性及偏移/形状误差，不能单独定位架构根因或自动宣布进入一致性训练。正式结果仍待AutoDL。

重叠诊断结果（2026-09-25）：[正式报告复核](BABEL_OVERLAP768_REVIEW.md)确认共同历史回读存在远高于数值噪声的窗口依赖，但较晚窗口部分指标更准确，且位置/上下文尚未单独归因。结束本轮诊断，下一建议仅做一组同预算原损失/共同历史一致性辅助约束比较，不直接匹配完整向量；尚未实现或启动训练，旧用途门槛和晋升结论不变。

一致性试验已准备（2026-09-25）：[固定权重同预算对照](BABEL_CONSISTENCY768.md)已实现并通过本地合成检查。四组从同种子的最后300轮完整状态恢复，原损失与加0.10共同历史一致性比较，各追加100轮；不匹配整个embedding，不更换架构。冻结旧best及原用途读出作为保持参照，原验证选模后一次完成best/last检查。真实训练待AutoDL，前述诊断不等于训练已成功。

一致性正式结果（2026-09-25）：[共同历史一致性复核](BABEL_CONSISTENCY768_REVIEW.md)确认四组各100轮完整完成、best均为总400轮。共同历史差异在训练位移1/16下降17.77%–23.79%，未训练位移64下降10.85%–11.94%；原全局综合误差相对同预算control增加0.135%–0.656%，重建与当前字段保护通过。316/328检查通过，12个失败全部是旧冻结历史用途读出保留；尤其seed42相对control额外代价13.26%/31.97%，不能按均值宣布升级。结果no_full_upgrade，保留原768冻结模型及读出参照，本轮候选暂不替换。下一建议仅冻结新旧模型、同预算重新校准线性读出以区分接口变化与线性可读性损失；尚未实现/运行，不自动加训或扩网格。

读出收尾已准备（2026-09-25）：用户同意后实现[冻结一致性状态同预算校准](BABEL_CONSISTENCY_READOUT768.md)。六份状态（旧best、普通续训、一致性各两种子）原权重冻结，各13目标×原五档Ridge，共390闭式候选；原输入基线精确复用并回放，全部78头锁定后评价研究集。既报告旧接口代价，也报告校准后对同预算control及旧best的5%主用途/10%任务族保留，以及原raw/PCA/current绝对协议。成功只保留需新头的稳定性候选；失败结束0.10配置，不加训或扩网格。25项相关测试通过、44旧源码哈希未变，真实清单祖先路径只读检查通过；正式提取与拟合待AutoDL，未提前宣称恢复。

读出收尾结果（2026-09-25）：[一致性768校准复核](BABEL_CONSISTENCY_READOUT768_REVIEW.md)确认390个闭式候选、78个选中头、0编码器更新，52/52恢复检查通过。候选新头相对同预算control主用途误差变化为原品种+0.95%/+0.01%、跨品种-0.95%/-0.72%；旧接口的明显代价可由同复杂度读出恢复。结合前轮共同历史稳定性增量，保留两种子一致性768＋各自新读出作为稳定性研究候选，结束本支线，不继续加训或扩网格。原raw/PCA/current完整用途协议仍失败（相对完整输入均值高8.40%/12.43%），旧冻结接口no_full_upgrade不改。下一步为固定版本状态接口交付，尚未实现；不自动替换512交付模型，也不宣称通用表示或未来能力成立。

扩容对照已准备（2026-09-25）：用户要求继续检验层数/头数容量后，实现[固定768状态的FFN与深度对照](BABEL_GROWTH768.md)。base两层FFN512、wide两层FFN3072、deep四层FFN3072，均8头，各种子42/43；保留128窗口、同一解码器及0.10一致性。各从对应一致性best400功能保持扩容，全部组统一重置两个AdamW并同样预热，各100轮/3800步/957800视图。六组选模锁定后统一校准12份best/last读出；升级须至少5%综合重建增量并保护稳定性、用途及当前字段，两个种子/研究集/best+last同时验收。33项相关合成/回归检查通过，45旧源码未变；正式训练待AutoDL。相同曝光不等于相同算力，100轮不证明收敛，本次不追加头数网格或自动替换交付模型。该受控容量试验优先于上一记录中的状态接口交付建议，旧用途失败结论不改。

扩容正式结果（2026-09-25）：[768固定状态容量复核](BABEL_GROWTH768_REVIEW.md)确认六组各100轮/3800步完成，代码和导出报告哈希、抽样与预算及完整判据复算一致。单纯两层FFN扩宽综合收益0.50%～0.91%；四层相对同预算base全局误差下降20.95%～32.77%、末端近期回读下降55.25%～58.96%、共同历史差异进一步改善，支持四层结构作为更强重建研究候选。正式no_capacity_upgrade不改：seed42原品种中途局部回读误差+6.61%，历史用途周级保留仍有未通过项，原完整用途协议仍失败；1502/1536不能作为晋升通过率。deep最佳均在最后100轮且验证仍下降，不能宣称已充分收敛或加深无效。下一建议固定四层、冻结编码器同预算适配共享局部头，区分读出适配和中途状态信息缺口；尚未实现/运行，不自动延长训练、扩头数或替换交付版本。

局部适配已准备（2026-09-25）：用户批准后实现[冻结两层/四层共享局部头对照](BABEL_LOCAL_READAPT768.md)。base/deep两个种子的growth验证best编码器固定，新头同规格同随机初始化，原配套头作为参照；原4789训练窗口、每窗四个非留出位置、100轮/3800步，各1,915,600次局部曝光。验证只用32/64/96/128；48/80/112不参与训练、统计或选模，全部四组选定best/last后才评价研究集。通过只支持局部读出适配恢复，不改变growth768正式判定或原完整用途失败。40项相关合成/回归测试通过，48份旧源码哈希未变；正式训练待AutoDL，没有在本地执行真实推理或神经优化器更新。

局部适配正式结果（2026-09-25）：[冻结新头复核](BABEL_LOCAL_READAPT768_REVIEW.md)确认四组100轮/3800步完成，全流程约5分44秒，全部best在第100轮；原局部头回放、样本/计划/预算与120条判据复算正常。结果local_readout_retention_unresolved，36/120通过；对新base为36/40，对旧头为0/80。deep新头留出误差相对自身旧头+9.56%～36.19%，base新头同样+12.13%～35.45%；失败不是四层特有，不能据此诊断embedding丢失信息。末20轮验证仍改善，新头与长期旧头的初始化/覆盖/优化不同，当前随机重训未完成因果定位。保留四层编码器＋原头的重建候选，原容量/用途失败判定不改；结束随机100轮配置，不自动续训。下一建议原头函数保持起步的受控冻结适配，尚未实现/运行。

跨周期优先级更新（2026-09-25）：用户批准先审计多周期共同历史，原局部头暖启动适配暂列备选。[本地CPU原始审计](BABEL_CROSS_PERIOD_AUDIT_REVIEW.md)完成1,251文件/421合约/4,360,913行核对；完整区间OHLCV和持仓严格一致率为15→30的99.78%、15→60的99.25%、30→60的99.66%。收盘/成交量/持仓在完整区间全部一致，OHLC差异与首根细bar零成交相关；部分覆盖和不一致记录不进入首轮严格配对。保留4层768＋原局部头候选；下一问题收束为同预算跨周期共同历史价格变化辅助约束，不拉齐整个embedding、不变架构。23项合成测试通过，50份既有源码哈希未变；本轮没有模型训练/推理/拟合，原晋升及用途失败记录不改。训练侧划分/预热/端点配对与正式实验预算仍待实现，不能将全量原始匹配数当作最终训练样本数。

跨周期远端确认（2026-09-25）：[AutoDL报告复核](BABEL_CROSS_PERIOD_AUDIT_REVIEW.md)通过，约25.87秒、退出码0；4份报告哈希、代码哈希、1,251份原始文件对本地哈希均一致，逐合约及全部汇总统计复算一致。本轮数据对齐审计收尾，不再追加同类审计；进入固定4层768维8头、保留原局部头的同预算跨周期共同历史约束设计。训练侧原划分/预热/主力筛选和相同配对曝光仍须在实现中锁定；本次0训练更新，不宣称跨周期模型增益，不改变旧晋升结论。

跨周期训练已准备（2026-09-26）：用户批准后实现[固定768同预算对照](BABEL_CROSS_PERIOD768.md)。两种子各control/cross，从growth原deep best500及原局部头出发；架构4层/8头/768/FFN3072不变，两组统一重置优化器，各100轮/3800步/1436700视图。两组同样读取两份15分钟重叠视图与一份同时收盘的30/60分钟视图，保持原监督及0.10同周期一致性，仅实验组多0.10共同历史收盘变化约束。CPU预检9909个训练候选；新跨周期评价固定stride16，因为旧稀疏窗口15→60仅20/11对，不降低50对/5周门槛，原能力评价窗口不变。四组全部锁定best/last后统一520候选Ridge校准，再比较新稳定性、真实历史误差及原重建/局部/用途/当前字段保护；30→60未直接训练。42项合成/回归测试通过，50份旧源码哈希未变，真实数据仅做CPU几何与因果特征目标复验；未在本地训练、运行真实模型或拟合真实读出，正式收益待AutoDL。预算结束不自动扩容、续训或改权重，旧晋升失败记录不改。

## 1. 我们到底在解决什么

阶段终点补充（2026-09-25）：用户明确不希望无限追逐无损重建。[退出标准与工作顺序](BABEL_RECONSTRUCTION_EXIT.md)将“冻结表示能保留预先声明的用途”作为重建阶段的实用终点；不是只比较各重建模型的排名。允许当前768进入用途验证，先判断已知失真的实际影响，再决定是否值得做低频解码改动。尚无新的用途验收结果，也没有更改已有门槛。

目标是：在每根bar收盘时，把截至这一刻可观察的价格形态和交易活动压缩成可复用状态。历史回读检查信息有没有保留；不同用途的冻结读出检查信息是否容易使用。未来预测是后续独立问题。

“能压缩”“能读出某几个量”“值得使用神经模型”是三个不同结论。可视化好看、规则标签读出准确、接口运行正确，都不能单独证明最后一个结论。

## 2. 已经得到的实质收益

- 同一历史重建协议下，attention候选相对GRU综合误差降低约26.5%，见[架构复核](BABEL_ARCHITECTURE_REVIEW.md)。它是该预算和候选集合内的结果，不是全局架构最优证明。
- 保持固定PCA解码架构和200轮更新预算，改变训练窗口采样后，综合误差在原品种/跨品种研究集下降39.86%/29.01%，两种子都有支持。这是明确的模型质量进展，见[采样复核](BABEL_WINDOW_SAMPLING_REVIEW.md)。不能把收益全归因于唯一bar覆盖或窗口位置中的某一项。
- 已交付的512维状态能回读较完整的价格轮廓，也能线性读出历史方向。16/64根方向三分类的平衡准确率约86%–88%。这是对截至当前历史的描述，不是下一根涨跌准确率，见[状态读出复核](BABEL_WINDOW_STATE_PROBE_REVIEW.md)。
- 滚动窗口、因果特征、合约边界和恢复接口已有实测一致性证据，见[交付复核](BABEL_WINDOW_STATE_REVIEW.md)。它解决可调用性，不增加表示能力。

因此不能说这些训练没有成果。但最近几轮主要是诊断与交付，没有产生新的主模型突破。

## 3. 当前方案是什么，又缺什么

实际路径是：单合约持续计算的28维因果特征 → 滚动128根 → 因果Transformer → 末端512维坐标 → 固定PCA逆变换 → 历史重建。最新窗口基线不是之前所有长短状态分支的叠加，也不是无限历史记忆模型。

固定PCA解码器只输出其512维仿射子空间内的结果。编码器是非线性的，但输出空间受限。普通PCA坐标是这个空间中对给定目标进行**不加权欧氏重建**的最优坐标；当前主指标却有缺失掩码、多尺度变化和不同分项权重，因此不能把PCA分数说成当前主指标不可突破的理论下界。学生已有部分价格指标超过PCA。

这也不是“只要换成非线性解码器就解决”的证据：原生512学习式解码器、残差解码和教师日程已经做过对照，未取得稳定全面收益。见[容量复核](BABEL_NATIVE_CAPACITY_REVIEW.md)、[教师复核](BABEL_PCA_TEACHER_REVIEW.md)、[退火复核](BABEL_PCA_ANNEAL_REVIEW.md)。

还有一个目标边界必须明确：输入包含当前已收盘bar，但历史重建主损失排除了当前bar。原意是避免当前输入复制，结果是“当前bar本身被充分保留”未被该损失直接监督。滚动128根输出解决了端点定义一致性，不能由此宣称已得到每个时刻完整、可变长度的历史压缩。未来修改训练目标时必须显式处理这一边界，不能悄悄改mask后沿用旧分数。

## 4. 简单方案的比较，究竟告诉了我们什么

下面均为已有研究集结果，神经模型数值是两种子指标均值，不是集成。当前交付模型的重建来自[交付报告](BABEL_WINDOW_STATE_REVIEW.md)，目标PCA参考来自[采样报告](BABEL_WINDOW_SAMPLING_REVIEW.md)，目标定义及研究端点相同。

| 重建指标，越低越好 | 当前512状态：原品种 / 跨品种 | 目标历史PCA512：原品种 / 跨品种 |
|---|---:|---:|
| 综合标准化误差 | 0.14018 / 0.08738 | 0.04067 / 0.01672 |
| 收盘路径MAE，近似bp | 17.87 / 11.51 | 18.59 / 13.80 |

含义是：价格轮廓已可用作研究原型，但整体历史保真仍有明显缺口，尤其实体和交易活动。不能写成神经模型全部落后，也不能用收盘路径一项取代完整目标。综合误差比值不是“丢失信息的倍数”。

必须区分两种PCA：

1. **目标历史PCA**压缩从已观察历史构造的七通道回读目标，是历史压缩的实际参考。它在窗口端点不使用未来，但采用不同的解析预处理，不等于与神经编码器拥有相同优化过程。
2. **输入PCA**压缩同一128×28输入，用于最近六个状态量的统一Ridge读出。它与目标历史PCA是不同任务，分数不能混用。

最近输入PCA对照中，神经表示的六目标综合读出误差均值更低，但两个种子、两个研究集的完整预声明条件未通过，方向任务族还落后于未压缩输入。故结论是**有局部增量证据，尚无稳定全面优势**。线性读出失败不能证明信息不存在；直接计算历史规则也不需要先训练神经模型。

## 5. 本阶段决定

### 768候选不能从阶段结论中省略

512是沿用原保留门槛的交付基线，不是所有指标上的最优模型。已完成同200轮预算的768_joint，在两种子best均值上，全局综合误差为0.104560/0.062616，相对512_endpoint的0.140181/0.087380改善25.41%/28.34%；采用同样全局解码后裁取近期的读取方式，近期综合误差为0.271646/0.183149，对应512的0.344625/0.230524。见[逐bar对齐复核](BABEL_BAR_ALIGNMENT_REVIEW.md)和[末端读取复核](BABEL_ENDPOINT_READOUT_REVIEW.md)。768有明确的综合与近期重建优势，不能用“未晋升”抹掉这些收益。

未替换的原因是原全局路径保留条件：768_joint种子43的best路径MSE在两个研究集分别增加13.91%/15.35%，超过预声明10%边界；其他部分检查属于区间无法确认非劣，并非全部已经证实退化。这是工程取舍，不证明512总体更优。768同时改变宽度、PCA rank，joint还改变监督方式，全部收益不能归因于维数。最近方向/波动读出和接口交付只验证了512，不是新的512胜过768证据。

后续若有合格新时间段，须同时冻结保留512_endpoint与768_joint两种子作重建确认，不能只测512而遗忘更强候选；仅有512完成的用途读出不得冒称已覆盖768。增加768用途读出需另行事前固定协议，不能在新数据上调参。原晋升失败结论不作事后更改。

保留512_endpoint两种子及其冻结接口作为研究基线；不晋升为通用bar表示，不宣布主线成功，也不否定整个表示学习方向。当前证据足够结束这批窗口重建、容量与读出诊断，不必追加实验才能做此判断。

停止把下列工作默认排为下一轮：增加维度/层数/EMA，继续搜教师权重或残差组合，为通过失败门槛而扩读出网格，以及用展示页面代替研究进展。现有模型未被证明充分收敛；停止的理由是证据与研究收益不足以支持继续无边界投入，而不是声称继续训练不可能改善。

下一优先事项是**冻结现有结果，在来源可核实的新时间段作一次确认**，而非再在旧研究集挑配置。它只回答现有进展能否保持，不能凭此制造“新架构突破”。实施前需锁定：

- 两种子原权重、归一化、PCA、读出参数和已有评分定义；不得用新时间段重新拟合或选择。若旧读出参数未保存，须先在旧来源恢复并核验，再接触新评分。
- 核实新数据未参与训练、验证和此前研究决策，按合约/周期处理；预热只能读取当时已存在的历史，既有输入与目标的重叠边界需明确记录。
- 一次并列报告历史回读与原六目标读出，保留目标PCA、输入PCA、完整输入和当前特征在各自任务中的位置；新时间段不是另一个“cross_research”名称。
- 沿用已冻结判据，逐种子报告价量分项、总体与有足够样本的分组及配对不确定性。仅平均值变好或部分指标通过，不改写成全面突破。

目前未核实存在合格新时间段，未实现或运行这次确认，因此不提供可直接开跑的命令、不宣称具备新留出。没有新数据时保持现有结论，不用继续复用旧测试集替代确认。

若确认失败，停止对当前配方的扩大投入；若确认保留有限收益，也只按有限用途保留。任何新的预训练方案都必须另立一个可证伪的问题，说明相对已做实验的新信息，并重新对齐“当前bar + 多尺度历史”的保留目标。不能把历史重建任务悄悄换成规则分类，更不能先承诺模型升级再寻找支持指标。

## 本次工作边界

本次复查源码与已有报告，更新阶段判断和主台账；未训练、未修改训练或接口源码、未重新选检查点。已有来源指纹与交付基线不受本次文档变更影响。
