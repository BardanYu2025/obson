# 冻结窗口状态接口：交付复核

2026-09-23收到 `babel_window_state_reports.tar.gz`，SHA256为 `46d5f9a21d5d33f459ca2863d73666a62ce61a6df36a896863f947316dc9e552`。对应实现提交 `637b377`。结论：现有512_endpoint基线的接口一致性验收通过，可以固定为本阶段可调用产物；这次没有提高模型能力，也没有改变原候选晋升失败结论。

## 完整性与复现

运行23:17:24至23:19:02，约98秒，退出码0、完成状态complete；编码器更新、读出头更新、统计拟合均0。种子42/43仍分别使用原best第196/199轮，不选择新种子，不做embedding平均。

本地独立核对：

- 当前报告15项可用文件hash、33项当前源码hash，以及上一轮来源153项文件hash一致。报告按设计省略两个模型权重，因此未在本地重算权重hash或执行真实模型推理；AutoDL完成标记及样例保留权重身份。
- 984个原品种研究窗口、453个跨品种研究窗口，各两个种子，全局/近期报告296个数值与上一轮逐项完全一致。AutoDL同时检查了逐窗口误差数组，本地报告没有重复导出这些数组，不宣称本地重新计算了全部误差。
- 固定MA/15、y/60、RM/15、ru/60四合约，每合约两个种子、各9个连续状态，共72次逐根检查。AutoDL报告增量28通道、滚动末端embedding、快照恢复最大差异均为0；原缓存窗口和追加未来不改特征前缀检查均通过。这里只验证这些固定序列，不外推为所有硬件/所有行情逐位相等。
- 本地检查24份导出状态：512维有限向量、127×7全局和16×7近期重建、原模型身份、bar收盘可用时间、排除当前bar、掩码和时间切片一致。独立从全局预测裁取并按预测锚点重新锚定，最大通道误差约2.26e-7；近期绝对价格与全局对应片段一致。
- 8个固定终点能与上一轮样例直接对照，近期原始通道最大差异约4.42e-6，通过原FP32容差。批次形状不同会产生很小数值差异，与本轮同路径恢复差异为0并不矛盾。

两份下载产物职责保持不变：`babel_window_state_reports.tar.gz`用于审阅；成功运行导出的 `babel_window_state_bundle.tar.gz`含两个冻结模型和验证凭证，用于留存和调用。当前上传的是报告包，没有检查用户下载目录中的模型包本体。

## 当前能力基线

以下为两个种子的指标算术均值，不是集成模型结果。研究集反复用于诊断，不作为全新独立留出集。

| 指标 | 原品种研究集，984窗口 | 跨品种研究集，453窗口 |
|---|---:|---:|
| 全局收盘路径MAE（bp） | 17.87 | 11.51 |
| 近期16根重新锚定路径MAE（bp） | 21.35 | 15.00 |
| 近期综合误差 | 0.34463 | 0.23052 |
| 近期单步变化相关性 | 0.628 | 0.623 |
| 近期单步变化标准差比 | 0.582 | 0.615 |

MAE按原代码的log百分比路径误差换算bp；不是未来预测误差。全局与近期各自使用约定锚点和范围，两行MAE不能直接解释为两种读出方法的优劣。

粗尺度历史结构可读出，近期逐根细节仍偏平滑。接口通过仅说明模型可以按严格定义逐bar调用、恢复并复现已有结果，不能把相关性理解成涨跌分类准确率，也不表示512维已完整保存全部行情信息。种子差异仍保留，不按本次近期指标重新挑选种子。

## 阶段收敛与下一任务建议

当前可冻结的是单合约滚动128根的末端状态接口。EMA和波动等预处理携带窗口之前的因果历史，神经编码仍只接收最近128根；这不等价于已实现全历史通用bar表示。详细约定见[BABEL_WINDOW_STATE.md](BABEL_WINDOW_STATE.md)。

下一步优先做一个明确用途的冻结表示验收：当前行情方向、趋势强弱和波动状态的轻量读出。事前固定因果历史定义、时间/合约隔离、两个种子和简单基线，以同一输入来源的原始特征读出与同维PCA读出作为比较，检验信息保留与跨合约稳定性。若目标可由历史直接计算，其精确计算值就是参照上限，不声称模型应当超过产生标签的公式；简单指标也必须注明是否直接参与目标定义。规则派生量只作为可解释诊断标尺，不作为重训主模型拟合规则的目标；能拟合一套规则不等于学会市场机制。尚未观测的新区间应作为独立确认，现有研究集只作探索。

本次仅完成结果复核并记录，不启动该用途实验、不添加分类头、不修改主模型，不再扩维/扩层/延长训练。具体任务和判据应在下一次运行前锁定，避免继续无边界搜索。
