# 量仓对齐固定第100轮诊断复核

2026-09-21收到`babel_activity_alignment100_reports (1).tar.gz`。
SHA256：`2369314baf2fbea5728747001dbc902cdcd718dd592ca1a98cd488896bde6c6d`。
本次仅分析报告，没有执行新训练、改变选模或替换主模型。

## 完整性与比较口径

- diagnose-last退出0，6组均固定epoch100；completion声明source_unchanged=true、diagnostic_only=true、automatic_promotion=false。
- 日志22:24:24至22:27:30，共3分06秒；编码器冻结，仅重新拟合读出头。
- 与上一压缩包逐字节比较，除导出日志、状态、说明文件外，107个原产物全部一致，原主次选模报告未被覆盖。
- 本文表格全部来自last_diagnostic，不混入价格最佳或历史最佳候选。984测试窗口，周配对共51周；all/clean相同，不能当作独立复现。

## 固定末轮结果

下表为两个微调种子平均，MSE越低越好；不表示独立预训练复现。

| 方案 | 历史线性MSE | 历史MLP MSE | 收盘MAE bp | 状态BA |
|---|---:|---:|---:|---:|
| control | 0.82847 | 0.83159 | 45.094 | 54.44% |
| aux005 | 0.80381 | 0.81186 | 44.341 | 54.41% |
| aux020 | 0.63732 | 0.64196 | 46.621 | 55.96% |
| 当前量仓输入读出 | 0.74394 | 0.73617 | — | — |

强约束相对同轮control的历史MSE下降23.07%/22.80%，相对当前输入对照下降14.33%/12.80%。两个种子、线性及MLP的过去目标周配对区间均完全低于0。这修正了上一份合格候选报告中的“尚未超过当前输入”结论：固定第100轮状态已经超过，但不具备完整升级条件。先前历史选择器选中的强约束第60轮结果没有改变。

强约束减当前输入的历史综合MSE及周配对区间：

| 种子 | 线性差值 [区间] | MLP差值 [区间] |
|---|---|---|
| 42 | -0.10923 [-0.14677, -0.07202] | -0.10357 [-0.13764, -0.07433] |
| 43 | -0.10401 [-0.13891, -0.07303] | -0.08486 [-0.11341, -0.05949] |

这支持状态保留了超过当前特征可读出的观察历史信息。当前输入含EMA上下文；不能把比较说成对完全无历史信息的单bar。目标也是本轮辅助监督直接优化的描述量，不能据此宣称新任务迁移或市场行为识别。

## 价格代价及边界

- seed42：control收盘43.861bp，强约束45.516bp，增加3.775%；周配对差值+1.656bp，区间[0.565, 2.934]。
- seed43：control收盘46.328bp，强约束47.726bp，增加3.017%；周配对差值+1.398bp，区间[0.448, 2.338]。
- 两种子状态BA保留均通过，但价格保留均失败；不放宽2%门槛、不用平均数覆盖失败。
- 强约束两组验证资格也失败。control seed43自身验证收盘比固定父参考高9.67%，强约束为12.56%。因此不能把全部晚期退步归因于辅助任务或容量竞争。
- control训练base损失从21～30轮中位数约0.138降至91～100轮0.105，但验证detail从约0.101升至0.102；存在训练与验证走势分离。当前证据不能区分过拟合、优化波动和表示/解码适配问题，也不能断言仅降低学习率便可解决。
- 1bar价格变化相关性：control平均0.3614，强约束0.3571；标准差比0.4520/0.4471。量仓改善没有顺带解决逐根价格细节。

## 保留了哪些历史信息

线性读出R²，两种子平均：

| 目标 | control | 强约束 | 当前输入 |
|---|---:|---:|---:|
| 过去1～16根相对成交活跃度 | 0.312 | 0.612 | 0.060 |
| 过去1～16根持仓变化描述量 | -0.024 | 0.435 | 0.081 |
| 过去1～16根持仓变化/成交量描述量 | -0.174 | 0.342 | 0.075 |
| lag4成交量对数变化 | -0.022 | 0.008 | 0.004 |
| lag16成交量对数变化 | 0.000 | -0.025 | 0.010 |

收益偏向近期统计状态；单根较远历史变化仍弱，不能称为无损编码，也不能一概否定历史持仓信息可学习。

## 阶段决策与下一步建议

量仓历史保留获得跨种子支持，完整升级未通过。保留既有主模型，同时保留强约束末轮作为有价值的研究候选。512维不足、GRU不合适、需要双分支等假设均未被本轮证明。

优先考虑固定编码器的价格解码适配对照，而非直接继续扩大编码器：

1. 固定control/aux020两个种子的第100轮编码器，共4组；输入、端点、状态和量仓信息不再更新。
2. 四组价格解码器采用共同来源、匹配初始化和同样预算/优化协议。不能只为强约束多训，也不能混用不同候选轮次。学习率、轮数、选模网格需在实现时先声明。
3. 同时保留未适配的原解码器结果。原价格门槛、固定参考和历史读出证据保持，不把已经退步的control末轮当成唯一保留参考。
4. 若价格可恢复，支持解码适配存在可修复因素；若不能恢复，只能说明这套适配方法/预算未恢复，仍不证明历史价格信息已丢失。
5. 后续再依结果决定匹配的学习率/损失平衡实验或结构分工，不同时改变特征、容量和目标。最终表示迁移仍需未直接监督任务及新的留出数据。

这是一份下一阶段建议，尚未新增对应训练入口，也未运行上述对照。
