# Babel 实验目标与阶段台账

本文件记录用户要求：每轮实验先对齐总目标、当前阶段问题和验收标准，避免指标驱动的方向漂移。更新实验结论时同步更新本台账。

实验设计、代码审查和结论分级遵循 [BABEL_EXPERIMENT_REVIEW.md](BABEL_EXPERIMENT_REVIEW.md)。测试通过与实验设计有效必须分别检查，有限预算结论不得扩大为方向性否定。

当前执行任务（2026-09-23）：[端点覆盖与分布审计](BABEL_COVERAGE_AUDIT.md)。本地原始数据和端点复现完成：4789个不重叠窗口覆盖612992根合约周期bar；同512门槛stride16可得到38549窗口，唯一bar仅增加11.64%。四分区目标/掩码指纹精确复现，输入x需AutoDL原数组逐元素核验。已准备CPU核验及自动导出；通过后再实施固定PCA/plain、同更新预算的窗口位置变化对照，未启动新训练。以下阶段记录按时间保留。

## 总目标

每根已收盘bar产生因果embedding，表达其之前可观察的行情历史及状态；既包含价格形态，也应保留有用的交易活动信息。短状态和长状态共同表达局部变化与更长历史。历史重建用于检验压缩信息的保留能力，后续分类或自回归是独立阶段，不能用历史重建成功冒充预测成功。

不要求每根bar猜出下一根涨跌，不要求以OHLCV/OI唯一还原真实买卖盘或开平仓行为；暂不把这些不可识别的含义硬赋予潜变量。

## 已有证据与边界

- 已形成长状态与512维短GRU状态的实验基线。长尺度价格形态重建较好，1bar变化相关性约0.36、标准差比约0.4，逐根细节仍弱。
- 自回归解码、增加EMA、降低影线权重等对照未证明稳定全面优于当前方案，不能因此不断扩大同类试验。
- activity512：增加量仓显式特征和保守输入分支未取得稳定价格收益；持仓变化线性读出R²约-0.13。不能据此断言量仓没有价值或embedding绝无相关信息。
- 测试时段被反复研究使用，当前报告属于研究集证据，定型后需要新的留出评估。

## 当前阶段：量仓历史保留的训练目标对齐

实现与运行说明见 [BABEL_ACTIVITY_ALIGNMENT.md](BABEL_ACTIVITY_ALIGNMENT.md)。

问题：在编码器容量和输入固定的情况下，加入观察历史的辅助重建约束，能否让状态更容易读出量仓变化，同时保住价格表达？

唯一主改动：量仓历史辅助损失。无约束/弱约束/较强约束，两个种子。冻结状态再进行线性与非线性读出；增加当前量仓输入对照，分开报告当前目标和不含当前bar的历史目标，审计异常记录敏感性。

阶段验收同时要求：历史读出跨种子改善、有配对证据、超过当前输入对照、价格重建和状态读出保留。训练loss下降、当前输入复制、单个种子最好值，不构成完成。

2026-09-21收到activity_alignment512报告：6组各30轮完成，源文件未变，总运行约19分47秒。辅助监督确实改善历史信息读出，但两档权重均未通过预先约定的完整验收，主模型保持原方案。

- 相对同输入control，辅助权重0.05/0.20的过去目标线性读出误差平均下降1.62%/3.52%，非线性下降1.00%/3.09%；两种子、两种读出均有周配对改善证据。
- 过去目标综合标准化MSE：线性control0.8353、弱0.8218、强0.8059，当前量仓输入对照0.7439；非线性分别0.8347/0.8263/0.8089，对照0.7362。因此未通过超越当前输入的综合标准。
- 不等于毫无历史记忆。过去1～16根相对成交活跃度的线性R²：control0.310、强约束0.344、当前输入0.060；lag4同项0.129/0.171/0.001。当前输入对照本身含因果EMA上下文，是已有特征的强对照，不是毫无历史信息的原始单bar。
- 价格/状态保留不稳定：弱约束seed42收盘误差+2.09%、状态BA下降1.05个百分点；强约束seed43收盘误差+3.85%、BA下降3.11个百分点。不能用跨种子平均掩盖退步。
- 全部984个测试窗口的17bar目标跨度都通过异常筛查，all和clean是同一个集合；不能声称获得额外独立稳健性证据。更早的输入前缀不在此clean定义内。
- 结论仅支持“监督能改变信息保留，但当前共享状态微调出现取舍”，不能证明512维不足、GRU无效或量仓无用。线性和本轮小MLP都未解决逐根持仓变化读出，不等于所有非线性读出都无效。

30轮阶段完成分析，升级验收未通过。但尚未充分检验训练预算，因此不能据此排除共享状态或优先改新架构。用户指出主模型训练预算差异后，复核确认：large512局部Transformer曾训练200轮，聚合30轮、联合20轮；后加短GRU训练30轮，最近实验是短GRU微调，不是对整套Transformer继续完整训练。

## 当前优先任务：同预算续训核验

见[BABEL_ACTIVITY_EXTEND.md](BABEL_ACTIVITY_EXTEND.md)。暂停新增结构，三组两种子从各自30轮last的完整状态延长到总100轮，不重置优化器/随机状态，不提高辅助权重。保留原价格选模，新增训练拟合/验证评分的量仓历史选择器，不能拿未训练的control辅助头loss选模。两种规则分开报告，不用测试挑赢家。100轮不自动等于收敛。

旧实验不是全部作废：同起点同预算对照保留其条件内结论；AR冻结编码器60轮不能否定联合AR训练；EMA的共同预训练有EMA历史偏向，none失败时epoch0不能当作训练后性能；新特征/零门控短微调不能排除从头充分训练的潜力。涉及最终方案选择时再补关键初始化与完整训练对照，不机械重跑所有历史实验。

2026-09-21收到activity_alignment100报告：6组各100轮完整，原30轮history完全保留，恢复验证差异为0，父产物未改动，运行46分50秒。预算质疑得到实质支持：较强约束的统一历史验证Ridge误差从第30轮约0.75持续降到第100轮0.6061/0.6140，尚不能宣布收敛。原价格选模和新增历史选模仍均未通过完整升级验收。

- 历史选模下两种子平均过去目标MSE：control线性0.8287/MLP0.8313，弱约束0.8043/0.8143，强约束0.7526/0.7515；强约束较对应control改善约9.18%/9.60%，两种子均有周配对改善证据。此收益同时涉及训练预算和新增选择器，不能全归因于多训练。
- 当前输入对照仍为0.7439/0.7362；强约束与其差异的周配对区间跨0，不能说已超越，也不能说已证明等效。辅助监督目标读出不是未见任务迁移证据。
- 历史选择器强约束两种子都选第60轮。seed42测试收盘误差比对应control高2.91%、状态BA低约1.38个百分点；seed43保留通过。原价格选择器的强约束则seed43保留失败。平均值不能遮蔽失败种子。
- 强约束第100轮的验证价格门槛失败：seed42收盘误差比固定参考高2.61%（门槛2%）；seed43高12.56%，base/change16/detail也不合格。报告未包含这两个最终状态的测试读出，因此尚未量清末轮信息收益及其代价。
- 全部984测试窗口all与clean仍相同，不能当作第二份稳健性复现。

下一步固定评价全部六组第100轮last，不继续训练编码器，不根据测试结果挑轮次，也不放宽门槛。独立last_diagnostic只做诊断，不替换主次选模、不自动升级。先区分“学到更多”和“满足价格保留”，再决定是否需要改变优化/信息分配。代码补齐不等于AutoDL诊断已完成。

2026-09-21收到固定第100轮诊断，完整复核见[BABEL_ACTIVITY_LAST100_REVIEW.md](BABEL_ACTIVITY_LAST100_REVIEW.md)。6组评价完成，原107个报告/数据产物逐字节不变。强约束历史线性/MLP MSE平均0.6373/0.6420，较同轮control下降23.07%/22.80%，较当前输入对照下降14.33%/12.80%；两种子两种读出的周配对区间均支持改善。因此“末轮状态尚未超过当前输入”的说法不再成立；之前第60轮合格候选的结论保留。

价格仍有代价：强约束两种子相对同轮control收盘误差增加3.775%/3.017%，均有周配对退步证据；状态BA保留通过，完整升级仍失败。control本身晚期也退步，不能将全部退步归因于量仓容量竞争。主要收益在过去16根统计状态，lag4/16逐根成交量变化仍弱。

本阶段确认监督及足够预算可以增加可读出的量仓历史信息，但尚未兼顾价格表达。主模型不替换。下一优先建议是固定control/强约束两种子末轮编码器，以共同来源解码器、匹配预算做价格再适配对照，区分可恢复的解码适配与仍待定位的表示/优化问题。尚未实现或运行新训练；不据此提前判定512维不足或增加新分支。

用户确认后实现价格再适配入口，协议见[BABEL_PRICE_READAPT.md](BABEL_PRICE_READAPT.md)：四组固定编码器，缓存端点状态；共同预训练解码器、各100轮、batch256、同样余弦学习率。原解码器、验证选中、固定末轮和固定父参考一起报告，失败候选明确标注，不把继承量仓读出算新复现。下一任务为AutoDL运行并检验价格恢复，不预写实验成功。

2026-09-21收到价格再适配报告，复核见[BABEL_PRICE_READAPT_REVIEW.md](BABEL_PRICE_READAPT_REVIEW.md)。四组100轮完整，冻结回放最大差异0，来源未改，选模重算一致。强约束两种子收盘误差45.516/47.726bp降至43.704/43.713bp，分别选头第20/24轮；量仓历史读出因编码器未变而保留。说明相当一部分价格退步可通过解码适配恢复，不能直接归因为编码器价格信息丢失。

相对固定父control，两组价格/detail都通过保留；相对适配后control，收盘误差仍增加1.778%/2.123%，均有配对残余代价，seed43超2%门槛，完整验收仍失败，不放宽门槛、不自动替换。所选头均早于25轮，训练loss继续下降但验证detail后期回升，不优先延长同一解码器训练。1bar标准差比仍约0.40，逐根细节未解决。

下一优先建议：固定价格control和量仓增强候选，转向未直接监督的历史状态任务及可获得的新留出评价，检验通用表示价值。避免把研究目标收缩成围绕同一重建门槛调参；规则仅作冻结读出评价，不回流编码器训练。新阶段尚未实现或运行。

用户确认继续后，准备[BABEL_STATE_TRANSFER.md](BABEL_STATE_TRANSFER.md)的探索性迁移矩阵：4类描述量×32/64/128根，current/statistics/4个冻结状态/4个状态加statistics共10种表示；统一Ridge和两组MLP读出种子，验证选模，记录任务族与增量对照。现有端点仍是研究集，原始文件清单只能提供未经验证的留出候选，不能宣布已有新独立样本。编码器不更新，旧价格保留未全面通过的事实继续随报告保留。本阶段尚未在AutoDL运行。


2026-09-21收到state_transfer512报告，复核见[BABEL_STATE_TRANSFER_REVIEW.md](BABEL_STATE_TRANSFER_REVIEW.md)。10组输入、40份100轮读出记录完整；20个MLP选模可按验证分数重算，无epoch0回退，30组逐窗口误差与汇总一致。本轮只训练冻结状态读出，编码器不变。

量仓增强aux对同预算control的新12任务平均MSE：线性0.53224→0.50856（改善4.45%），MLP0.52499→0.50191（4.40%）；两个编码器种子、线性与两组MLP的主指标周配对区间均支持改善。aux+statistics对statistics的MLP平均0.47034→0.42521（9.60%），两种子三种读出均有配对支持。因此辅助训练收益不只限于原20个辅助监督目标，且相对本协议简单统计有互补信息。

边界必须保留：单独aux的MLP点估计仍落后statistics，配对区间跨0；aux+statistics对control+statistics不是所有配置都有明确配对改善。seed43方向持续性存在两项探索性退步；统计基线选97/100轮，不能宣称全部读出已收敛。原价格保留seed43超2%门槛的事实不变，不自动替换。当前测试仍是反复使用的研究端点，不能称独立泛化。

下一优先建议为完整来源审计和冻结候选的独立留出评价：扫描有RM/au/c/cs/ru五个品种共285份未经验证候选，只证明不在一份long_run清单，不能证明此前所有训练/研究都未见。先核对来源与质量，再锁定配置并一次评价价格、量仓和迁移任务；无合格新数据则如实说明。新阶段尚未实现或运行，不把本轮正向信号扩大为通用表示完成。


2026-09-22用户确认后，准备[BABEL_STATE_HOLDOUT.md](BABEL_STATE_HOLDOUT.md)：先递归追溯可用manifest来源，核对历史原始CSV快照，排除已注册品种、其他已锁定留出运行用过的品种、重复内容和质量失败。不能确认资格则退出码3并自动打包，绝不换名字宣称新留出。

合格后一次评价四个冻结编码器的原价格头、量仓历史Ridge及10种表示的12任务Ridge/两种MLP。迁移MLP复用原权重；未保存系数的Ridge只用旧训练数据和原已选alpha恢复并核验旧测试误差；旧量仓MLP未保存权重，本轮不伪称可直接复用。评分前锁定来源、审计与读出hash。只做跨品种、原test时段条件下的留出，不是新未来时间；注册表外未记录的研究无法由程序排除。没有神经优化器更新，不改变原价格门槛，不自动替换。本地合成检查与非训练回归通过，真实AutoDL运行和跨品种结论仍待报告。


2026-09-22收到state_holdout512报告，复核见[BABEL_STATE_HOLDOUT_REVIEW.md](BABEL_STATE_HOLDOUT_REVIEW.md)。完整运行exit0，36份注册清单及10个可达祖先审计通过，966个唯一源key；五个候选品种285份CSV均合格，得到453端点。资格限于可用记录与声明来源，不证明外部/已删除研究未见；这是跨品种、原test时间段评价。本地复核21项可用文件hash、30组迁移汇总和128组全部分组配对区间一致。

量仓过去15目标在共同452端点的线性MSE：control1.1720/1.1737，aux0.7692/0.7785，改善34.37%/33.67%；相对当前输入1.0012改善23.18%/22.25%，总体配对证据支持。五品种对control点估计均改善，但ru仍明确落后当前输入基线，不能泛化为逐品种逐任务优胜。收益主要在历史统计，lag4/16成交量变化仍弱。

新12描述任务的跨种子三读出综合迁移和统计增量标准均未通过：aux对control六项主比较仅seed42/MLP1702区间小于0；aux+statistics相对statistics的四个MLP区间都跨0。单独aux的MLP则四项均明确落后statistics。保留线性互补、价量联动等局部证据，不把未通过等同完全无信息，也不延续上轮全面迁移的乐观解释。

价格收盘误差control29.107/28.885bp、aux29.796/29.688bp，退步2.365%/2.780%，两组均超过原2%门槛且有配对残余代价。原主模型不替换，增强候选保留其量仓用途。

下一建议先复用两种冻结状态，检验价格control与量仓aux的组合增量；用原价格头读取control部分保证价格路径，明确这不是学得的价格改善。需要统计及等维双价格状态对照，披露1024维/两路推理成本，先原train/val读出再决定是否值得压缩或扩展。不立即扩大编码器。五品种已打开，后续使用其反馈即作为研究集，不能重复称未见确认数据。新阶段尚未实现或运行。


2026-09-22用户确认继续，准备[BABEL_STATE_COMBINATION.md](BABEL_STATE_COMBINATION.md)。18种输入×迁移12任务/量仓过去15任务两类，共36个读出worker、144条100轮MLP轨迹，默认4路并行。新增control+aux和等维control+另一个control，两个种子交换第一价格状态位置，并各自加statistics；单状态和统计基线全部按同协议重拟合。两个price_pair是同一对control的不同排列，不能当独立编码器复现。

编码器、价格头均冻结；价格路由只读第一段control，两个研究集逐批验证aux扰动不影响价格输出，明确是结构保证而非学得的保留。组合需两路512维状态和推理成本，不宣称同容量升级。只用原train/val选择Ridge与两个MLP种子的epoch/decay，选择锁定后才读两个评价集；五品种命名cross_research，不冒称新留出。保存Ridge系数、MLP权重/归一化、完整history和来源代码hash。100轮仍只是预算，末轮最优明确标记。本轮尚未在AutoDL运行，不能预写组合收益。

2026-09-22收到state_combination1024报告，复核见[BABEL_STATE_COMBINATION_REVIEW.md](BABEL_STATE_COMBINATION_REVIEW.md)。36个worker、144条100轮history及72个验证选择完整，无epoch0回退；本地362项可用文件hash、216组误差数组、864组任务族对照及其全部22896个分组配对区间一致。三份代码hash匹配25e9490，编码器未更新。旧10种表示60项迁移基线复现；价格四项路由差均0，是结构保证而非学得保留。包内无权重/原始数组，本地未重做前向。

组合确有信息互补：量仓任务上两个研究集的dual均在两种子三读出中胜过等维双control；cross上也全部胜过aux，线性平均0.7738→0.7168、MLP0.7995→0.7726。但旧test量仓非线性相对aux未稳定改善；迁移任务相对aux仅线性一致改善，八项MLP区间均跨0。

不能升级为通用1024维表示：迁移dual+statistics的MLP比aux+statistics更差，旧test0.4252→0.4381、cross0.4228→0.4444，八项中六项有配对退步证据。量仓dual+statistics也未稳定超过statistics；cross线性0.6523对0.6559的两种子区间跨0，MLP0.6900对0.6758。保留线性和价量联动局部收益，不用弱control基线掩盖强统计对照。之前aux单独价格保留失败继续有效。

训练预算不是本轮首要解释：dual迁移选19轮、加统计选28～35轮，量仓选5/11轮，后续训练loss降而验证明显变差；不能盲目延长。statistics迁移仍选97/100轮，不能概括全部收敛。训练4789端点，dual迁移读出132748参数、统计9484；维数/读出成本必须披露，只有dual与price_pair严格匹配。不能从探针失败推断信息不存在或模型容量不足。

阶段决定：不立即扩大或蒸馏组合，保留control价格主路径、aux量仓候选。下一建议回到有顺序的历史压缩，先设计按历史位置查询的联合价量轨迹回读和透明历史压缩/顺序敏感性对照；若确认时序信息缺失，再在明确预算下训练编码器。描述规则仅作诊断，统计胜出不等于K线表示方向失败。两个评价集均为已打开研究数据；新任务尚未实施，不预写收益。

2026-09-22用户确认进行真正主干架构比较，准备[BABEL_ARCHITECTURE.md](BABEL_ARCHITECTURE.md)。共同128根、28维因果输入，末端512维，三种从头编码器：2层512宽GRU、4层256宽因果Transformer、8层256宽多尺度因果卷积。编码器参数约317万/330万/332万；同种子共用完全相同初始解码器，均只从单向量和固定位置重建，不继承旧模型优势。它不是持久流式/2048根系统的替换，不直接比较旧模型不同目标的分数。

新目标为已观察价格路径、1/4/16/32根变化、实体与五项量仓轨迹，训练SmoothL1、验证同权MSE，末端当前bar不计分，缺失/异常量仓目标带mask，全部归一化只用train。3架构×2种子×2档学习率共12组200轮，effective batch128/micro64，默认2路并行；同样窗口顺序、更新数和验证选择，但不能声称相同算力/都收敛。保存全部best与last、选模锁、完整history、来源hash、参数量和实际耗时。

加入train均值、窗口均值7维、粗采样112维、训练拟合PCA512等透明基线；检查错配状态和保留当前片段的历史块排列扰动，报告多尺度/远近位置/价量分量。扰动仅为合成敏感性，不当作真实行情或完整时序理解证据。两个数据集仍是已打开研究数据，按周配对、逐种子/分量报告；即使有探索收益也不自动升级。正式训练与GPU验证只在AutoDL，本地无优化器更新；新结果尚未获得。

2026-09-22收到architecture512报告，详见[BABEL_ARCHITECTURE_REVIEW.md](BABEL_ARCHITECTURE_REVIEW.md)。12组×200轮完整，91200次更新，六个选择均为lr3e-4；GRU选149/143、attention184/173、multiscale193/187轮。41项可用文件hash、200个误差均值、216组分量对照及5724个分组区间复核一致。运行计时覆盖约2小时3分，之前4～10小时粗估偏长；来源最初校验时间未包含。

attention相对GRU综合MSE平均旧test0.42330→0.31129、cross0.27599→0.20280，改善26.46%/26.52%；两个数据集两种子及路径/变化/实体/量仓四分量均有配对改善。单根变化相关性从约0.23/0.27提高到0.49/0.52。multiscale也改善综合分数，但路径存在退步，最近量仓细节较强。选择attention为本协议优先候选，不宣称普遍最优或直接替换长历史模型。

必须更正本轮容量口径：attention/卷积内部末端256→名义512，共同decoder在位置展开前又512→256，神经重建通路实际连续维数上限至多256。PCA512综合MSE0.04067/0.01672远优于所有神经候选，但不是严格同有效容量，不能归因于512神经表示不足，也不能把全部差距归因于这一处瓶颈。此前未完整审查这一点是设计遗漏。配置间端到端结论保留，原协议加注更正，审查规范新增有效通路要求。

下一优先建议先补PCA256和有效瓶颈审计；真正512容量对照须同时修正编码器内部末端与decoder入口，披露参数量改变、保留共同预算与验证选模。仅改输出Linear或仅扩大decoder都不能冒充修好了两端。低学习率若干trial仍在下降，不宣称所有训练收敛；高学习率选中候选末期略退步，不优先盲目延长全部训练。两个研究集已打开，不作为新留出。新修正实验尚未实施，现有模型和代码不自动替换。

## 每轮实验前后必须回答

1. 这轮在总目标中解决什么具体缺口？上一轮哪项证据支持继续？
2. 主改动是什么，控制了哪些数据、初始化和预算差异？
3. 是否把目标缩窄成只会重建价格曲线、只会分类或只会复制当前输入？
4. 选模是否仅使用训练/验证，测试结论是否区分研究探索与最终泛化？
5. 实际通过了哪些验收，哪些仍是推测？若无稳定收益，是否应停止当前分支？

当前阶段结束后先据报告回答这些问题，再决定下一步。不得把持续加特征、加层数或追逐单项最低loss作为默认路线。

## 2026-09-22：原生512维与解码入口秩对照已准备

- 本轮问题：旧重建差距有多少与入口容量受限有关？只读复现PCA512，再补评同训练基底的PCA256；不得根据补评分数临时改变训练矩阵。
- 3种原生512维编码器×rank256/512×seed42/43，共12组，从头200轮；共同输入/缓存/损失/有效batch128，lr3e-4固定来自上一轮验证选择。rank配对共享全部初始参数，公共解码器通过两个256维记忆token读取状态。256分支在正交坐标中显式置零，避免名义低秩矩阵的浮点舍入通道。
- 旧架构源码及源运行保持不变。新旧编码器和解码器同时变化，不能将新旧差异全部归因于瓶颈；严格容量对照在本轮同架构同种子内部。近似匹配参数，不宣称等算力、充分收敛或架构全局最优。
- 两研究集已多次打开；所有组完成后锁定验证选模，报告最佳及固定末轮、目标族退化、逐窗口/周配对、PCA两容量、固定示例。无自动晋升，也不把本轮当作长历史流式替换。
- 新增因果前缀、最旧bar卷积依赖、真实坐标瓶颈及零空间、共同初始化、源文件只读/PCA复现、选模锁定、恢复与失败打包测试。本地仅合成前后向和无优化器更新流程；正式GPU验证及训练在AutoDL执行。

## 2026-09-22：capacity512报告已复核

12组各200轮完成，数据/源码/选模记录一致；约2小时2分。rank256→512使attention综合MSE种子均值降低test3.73%/cross6.60%，卷积4.64%/6.87%，四个配对区间分别均支持改善；GRU均值退化1.79%/3.19%。Attention仍有test实体、cross路径的个别退化，卷积无显著目标族总体退化不等于等效保留。

PCA256综合MSE为0.16431/0.09952，新attention512为0.30828/0.18954；这不能全部用入口维数解释，也不能忽略PCA直接编码解析构造目标序列的任务便利。新attention相对旧候选量仓改善约8.3%/8.5%，收盘MAE却增加13.4%/9.5%，因此保留两个研究候选，既有主模型不替换。

全部最佳epoch在95–131，后续训练误差下降、验证误差回升。停止本配置的盲目延长预算；下一步优先冻结状态的线性/非线性历史读出和PCA系数→同款解码器校准，附带原始极端窗口回查。保持同一训练/验证来源及两种子，不根据研究集挑头，不预设继续加层、加维度或改标签。详细证据与复核边界见[BABEL_NATIVE_CAPACITY_REVIEW.md](BABEL_NATIVE_CAPACITY_REVIEW.md)。

## 2026-09-22：PCA教师实验代码已准备

用户确认以PCA辅助学习，但学生评价必须完全通过自己预测的512维状态。教师基底及坐标标准化只来自原训练集；输入保持原28维因果特征，完整窗口的PCA重建不能倒灌早期bar。

基础阶段plain/teacher两组×两个种子，共4组各200轮，共享固定PCA解码器和初始化，只有坐标辅助损失不同。验证锁定父检查点后，再做10组各100轮的固定头继续训练、残差开启、教师权重减弱对照；兄弟分支共享父权重/零残差初值/数据顺序/优化器重置和学习率，记录实际继承epoch。跨父组有效预训练步数可能因验证选模不同，不冒称完全相同。

旧冻结读出及旧双token头校准优先级暂后置；本轮使用已知PCA逆变换做数值闭环，并设置完整控制组，检验是否更容易学到已观察历史的压缩编码。以真实历史主指标选模；坐标误差只是辅助。仍要求价格和量仓分项保留，报告所有种子、epoch0与固定末轮，不把教师输入诊断当学生成绩。自动导出 `download/babel_pca_teacher512_reports.tar.gz`。235项本地无正式训练回归检查通过，实际GPU验证与训练在AutoDL。

## 2026-09-23：PCA教师报告复核

4×200轮父组、10×100轮分支完整完成，共68400步，约1小时1分29秒。源码/缓存来源/14份历史/10条谱系与选模锁一致；重算380个均值和384个指标对照、10176个分组区间。归档无权重/坐标数组，本地没有复跑真实推理。

同200轮plain（无坐标辅助，固定PCA逆变换）相对前轮attention512综合MSE减少24.54%/35.10%，变化、实体和量仓均有两种子两集改善证据；这是方案背景比较，包含PCA先验/投影/标准化等差异，不能只归因于某个模块。逐bar变化相关性约0.640–0.649/0.673–0.690，价格路径MSE仍有代价，PCA解析基线仍明显更好。

teacher−plain为seed42退化、43改善，收盘MAE四组都退化；坐标更准不等于加权主任务更好。teacher_residual选0/0即保留已训练父模型，绝非没跑。teacher_release选46/10，λ仍为0.0204/0.2041，不能宣称成功完全释放约束。plain_fixed等预算续训综合改善但cross实体退化，仍不得自动晋升。

本轮plain父组最优200/200，固定头分支100/97，不套用旧方案的收敛判断。教师早期两个种子验证更好，当前释放直到约200轮才开始；建议只补固定头早期退火两种子，与既有200轮plain/constant-teacher对照，不再扩残差矩阵。按验证曲线提出假设，不能预写结果；同时回查少数极端窗口的原始行情及目标构造，研究指标完整保留。


## 2026-09-23：准备早期退火与路径核查

按[PCA教师复盘](BABEL_PCA_TEACHER_REVIEW.md)只补anneal_s42/s43，各200轮，与已经完成的四个200轮plain/teacher组对照。固定512维模型、PCA逆变换、训练数据与顺序、batch128/micro64、原LR3e-4日程。唯一实验变量为坐标监督权重：0.25保持至50轮，线性下降至100轮0，此后为0。AdamW连续使用，恢复加载优化器/RNG/绝对epoch，完整预算15200步。

旧文件只读核验，复制原训练坐标及统计而非重拟合；新初始化复现旧教师组epoch0验证。所有新组选模锁定后，复跑四个旧组best/last，逐窗口核对原报告；同时报告新组best/last及选中λ。以相对plain的两个种子/两个研究集综合改善与分项保留为探索性门槛，不把未检出退化当等效。

两端点路径诊断核对原始CSV、log/asinh往返、缓存特征/目标，不删除极端窗口。原始文件缺失或快照不匹配明确报告；缓存目标不能自洽时报错并保留诊断。本地原始CSV编码往返正确，实际AutoDL缓存匹配待运行验证。

36项相关合成/回归检查通过，包括未做神经更新的完整流水线、只读控制组、日程/步数保护、带合成AdamW动量的恢复、初始化复现、研究集读取顺序、失败自动导出。原512配置合成前后向通过（无优化器更新），GPU正式训练/推理尚未执行。旧pca_teacher及之前源码指纹保持不变。


## 2026-09-23：早期教师退火报告复核

两个新种子各200轮、7600步，共15200步，13分57秒完成；前50轮训练/验证指标与同种子constant-teacher逐项完全一致。17份根报告、来源和控制文件指纹、两新组历史/初始验证/选模锁核验，重算160指标均值、48项配对指标和1272分组区间；旧控制/PCA在原容差内复现。报告无权重/优化器/原数组，未重跑真实推理。

anneal_s42相对plain的综合误差test+4.50%/cross+3.98%，s43为−4.03%/−8.19%，四个周区间均支持各自方向。两个选中模型196/198轮λ=0，真实完成监督退出；固定末轮结论一致。相对constant-teacher，价格路径/变化/收盘MAE四组改善，但量仓四组退化，没有全面稳定收益。不挑seed43，不平均掉seed42的分项损失。

两极端端点原始CSV、缓存特征和目标matched，最大价格目标差约4.72e-6/1.34e-6对数百分点；本地CSV指纹及编码往返结果复现。只能排查这两个窗口的数据处理一致性，不能认证原始行情真实性或推及全部数据无误。

遵守原停止规则，结束教师日程/残差组合搜索，保留plain固定PCA研究基线。下一建议先审查4789训练窗口的原分区覆盖、重复度和分布支持，再决定是否增加原训练端点；模型、PCA/归一化、训练预算和评价协议的变化必须分别控制。当前只记录复盘，没有启动新训练。


## 2026-09-23：原始数据覆盖审计

完整核对966份原始合约周期文件/3370650行及285份跨品种文件/990263行。原训练端点精确复现为lagged-main且end=127 mod128、分区内至少512根历史；该条件继承自旧层级模型。train/val/test窗口原始行交集0；现有4789训练窗口在同合约周期内不重叠，覆盖612992根bar，不得把窗口数当bar数或统计独立交易数。

保持512门槛、stride16得到38549窗口、684320根唯一输入bar（+11.64%）；同时降低历史要求至128则39527/701840（+14.49%）。只去512门槛保持stride128为4912（+2.57%）。加密主要增加重叠观察切片，不支持直接八倍窗口×200轮后与旧基线归因比较；移除历史门槛会改变低成交历史比例，应另行控制。

本地四分区y/mask共8个文件SHA256与原缓存一致，训练目标尺度精确复现；输入统计最大差约4.6e-13，但x文件指纹不同且归档无原x。明确本地reports_only，未宣称全部输入一致。AutoDL CPU脚本核实际缓存哈希及每通道数值差，失败保留诊断并自动打包。42项相关检查通过，所有旧训练源码指纹不改。

下一建议保留原模型/PCA/统计/plain损失及512历史筛选，仅从stride16候选池每轮抽4789个窗口，总200轮、7600更新/种子，与旧plain更新步数和窗口曝光匹配。其研究问题是位置切片多样性，不是更多独立市场历史或新的留出证明。本次只完成审计代码和结果，训练实验尚未实现/启动，先等待实际输入缓存数值核验。

## 2026-09-23：AutoDL覆盖审计通过

严格运行退出码0，reports_only=false；train/val/test/cross四分区x/y/mask共12份数组重建SHA256与源缓存一致，最大数值差0，原训练统计也精确复现。本地此前输入哈希不同未在实际训练环境复现。核对11份根报告、7份源码、17份来源报告，重算7204窗口覆盖与176组分布摘要，全部一致。

保持512门槛、stride16的候选为38549窗口/684320根唯一合约周期bar，比旧覆盖增加11.64%；不是八倍独立历史。批准进入固定预算采样实验的实现阶段：每轮均匀无放回抽4789，跨轮允许重复，200轮/7600步/种子，42/43；原模型/PCA/统计/学习率日程、验证和研究端点固定，原plain两组只读复现。记录候选端点、采样指纹、累计唯一覆盖和品种/周期/时间曝光；包含epoch0和固定末轮，锁定验证选模后评价研究集。采样策略仍包含少量覆盖及曝光分布变化，不独归因于窗口位置。

研究目标仍为已观察历史的因果压缩表示，审计通过不是模型提升或主模型晋升。没有新训练更新；此条记录协议，训练实现尚未交付。完整证据及边界见[BABEL_COVERAGE_REVIEW.md](BABEL_COVERAGE_REVIEW.md)。

## 2026-09-23：固定预算窗口采样实验已准备

实现sampled_s42/s43两组并行从头200轮，与旧plain_s42/s43只读对照。保持原512编码器、固定PCA/统计、28维输入、plain目标、batch128/micro64和原学习率日程；只从原train且保留512历史门槛的stride16池每轮均匀无放回抽4789，38步/轮，7600步/957800窗口曝光/种子。旧窗口在新缓存中逐元素复现，原始数据指纹和完整预算核验，初始验证复现，恢复保留优化器/RNG/绝对采样轮次；候选索引指纹绑定检查点。

锁定所有新组选模后评价原研究集，同时报告best/last两套指标、逐窗口误差和按周配对区间，复跑旧plain best/last及PCA控制。采样计划导出各轮分层曝光、唯一窗口/bar覆盖与最终每候选抽取次数，正常或失败均打包到download/babel_sampling512_reports.tar.gz。无自动模型晋升，不改旧源码及源缓存。

55项相关本地检查通过（13项新增）；正式512维模型合成前后向/因果前缀通过，无神经优化器更新。真实原始数据及两种子200轮采样预演完成：累计均覆盖38549端点/684320唯一合约周期bar；首轮仅覆盖412688/409840唯一bar，低于原窗口612992，因为密集窗口在单轮有重叠。不得把固定窗口曝光称为固定每轮独立信息量；本轮比较包含位置、少量覆盖及曝光分布变化。AutoDL正式训练与实际权重推理待运行，详见[BABEL_WINDOW_SAMPLING.md](BABEL_WINDOW_SAMPLING.md)。

## 2026-09-23：固定预算采样报告复核

两组各200轮/7600步/957800曝光完整完成，约15分23秒；均验证选中199轮。全部4789原窗口在新缓存的x/y/mask精确复现。核对源码、18根报告及附带依赖/worker指纹，重放400轮采样计划，重算200指标均值、48配对指标/1272分组、80组旧控制/PCA逐窗口数组。报告无二进制检查点，本地没有真实模型推理。

sampled相对plain的最佳轮综合误差种子均值test下降39.86%、cross29.01%，固定末轮下降39.97%/29.01%。两个种子/两研究集/最佳与末轮的六个主要误差，其全体周配对区间都支持改善，通过预设探索性门槛；保留两个sampled为优先研究候选，主模型/长历史流式系统不自动替换。位置、部分覆盖、曝光分布共同变化，不独归因于位置。

一步变化相关性test约0.787–0.788、cross0.824–0.826，但标准差比降至0.716–0.718/0.804–0.805，较旧模型更收缩；不能说所有诊断指标改善。实体/量仓与PCA512仍有大差距，末端附近细节也仍较弱。验证150至200轮仍下降约6%，不能宣称充分收敛。

下一建议优先冻结新旧编码器，做同协议的不同前缀位置历史读出，验证每根bar状态而非只验证h[127]。因果性不等于可读性，读出只是表示诊断，不把训练目的改为拟合趋势规则。目前只完成复盘，未实现新诊断实验；不同时扩容量/改目标/延预算。完整证据见[BABEL_WINDOW_SAMPLING_REVIEW.md](BABEL_WINDOW_SAMPLING_REVIEW.md)。

## 2026-09-23：冻结逐bar状态诊断已准备

固定plain/sample两种子均为第200轮权重，不混用验证最优199与末轮200。诊断第32/64/96/128根的状态，输入严格截断至当前bar；读取此前16根已观察价格路径、实体和量仓，排除当前bar并重新锚定路径。四编码器加current28因果摘要基线，共20格，每格ridge和MLP；共享原4789/978固定train/val来源，MLP同种子同位置配对初始参数指纹一致，各100轮/1900步，共38000读出更新，编码器0更新。目标尺度只在train四位置合并拟合，输入统计只在各格train拟合。

额外将128位置的已拟合头及其统计直接用于早期状态，无新增拟合；区分位置单独可读与共享读取能力。位置也改变上下文长度和目标片段，不能独归因于位置编码。原窗口选择及周/月库存继承自128端点，本轮是离线同窗口诊断，不冒称在线逐bar无选择偏差。

20格全部完成/验证锁定后才提取研究状态，报告所有位置/种子、ridge、MLP最佳及末轮、迁移结果、相关性/幅度/分项/逐窗口与按原端点周配对区间。无自动晋升，不加入趋势规则标签，也不把探针局部主指标与旧128重建主指标直接比较。正常或失败自动导出download/babel_prefix_readout512_reports.tar.gz。

64项相关本地检查通过（9项新增），合成前后向与闭式求解/无神经更新完整流程验证来源、因果目标、选模、恢复和导出。旧源码保持不变，真实冻结状态提取、CUDA求解和读出训练待AutoDL运行。详见[BABEL_PREFIX_READOUT.md](BABEL_PREFIX_READOUT.md)。

## 2026-09-23：冻结逐bar状态诊断报告复核

20格全部完成，各MLP100轮/1900步、合计38000次读出更新，四个第200轮编码器保持冻结；约4分12秒。核对119项可用文件指纹、源码/上一轮来源、2000条训练历史及配对初始化/选模锁；重算1128指标均值、1152配对指标/30528分组结果。176项二进制引用未附带，不能本地重跑模型或独立复核检查点内epoch0数值；AutoDL源权重与验证回放通过记录同导出证据一致。

sampled同位置ridge在32/64/128位置两个种子/两研究集均改善；96位置不稳定，seed42跨品种综合误差退化约7.10%，周区间支持退化，不能平均掉。32个编码器/位置/研究集ridge均优于current28；当前MLP在验证及两研究集全部16状态格均弱于ridge，并出现训练继续降、验证升的过拟合。该结果不支持直接扩大读出器。

128位置头零拟合用于32/64/96：原/新四编码器与ridge/MLP共48个研究集比较均明显退化，且高于train均值基线。sampled ridge种子均值误差为位置独立头的3.25–7.64倍。说明各位置虽有可读历史，尚未得到直接通用的读出方式；不能断言全部信息丢失、单独归因于位置编码或要求扩容量。

下一建议先冻结模型做多位置共同训练的共享历史读出，并检查未训练位置；若仍不足，再在原全窗口重建上加多位置历史重建监督，同时保留同起点同预算原目标延训控制和两个种子。当前只复盘，未启动新训练；sampled仍是窗口末端优先研究候选，通用逐bar/主流式模型不自动晋升。完整证据和限制见[BABEL_PREFIX_READOUT_REVIEW.md](BABEL_PREFIX_READOUT_REVIEW.md)。

## 2026-09-23：共享读出与线性容量参照已准备

继续冻结plain/sample两种子第200轮，保留current28对照；共享ridge/MLP仅在32/64/96/128位置train拟合、同位置val选模，48/80/112仅用于共享头的位置插值评价。目标仍是当前之前16根局部路径/实体/量仓，源训练尺度不重拟合；新位置严格截断，源四位置数据及独立ridge结果复现。位置独立的参考头另行拟合于保留位置train，使用自己的val选alpha，完全不进入共享模型拟合或选择，不能说所有头都未见保留位置。

每共享MLP100轮、19156行/轮、batch256、7500更新，五头37500更新；编码器0更新。相对四个旧位置独立头，聚合行曝光相同，更新数7500对7600、容量/约束不同，明确不称纯共享因素或严格等算力比较。训练位置与保留位置分开报告，先在原窗口内聚合位置再按原端点周配对，避免把位置当独立样本。导出epoch0、最佳/末轮、验证回放、尺度、全部历史和来源指纹；恢复保留优化器/RNG/绝对轮次。

额外只在原train目标拟合一次嵌套PCA，预定32/64/128/256/512/768/896维，完整报告原全窗口目标的验证与研究误差；512必须复现旧PCA控制，896作完整空间数值往返。此参照不是全部28维输入的信息维数，不是神经可训练性或预测能力证明，不用研究数据选维度。编码器容量和监督本轮均不改变。

65项相关本地检查通过，包含7项新增：因果目标、共享拟合位置隔离、合成PCA、无神经更新完整流程、源文件不变、原对照回放、epoch0锁定及篡改拒绝、优化器恢复、失败/重导出。旧训练源码不改。真实提取、读出拟合和真实PCA曲线待AutoDL运行，协议及一键运行/自动download打包见[BABEL_SHARED_READOUT.md](BABEL_SHARED_READOUT.md)。

## 2026-09-23：共享读出与容量曲线报告复核

五个头各100轮/7500步，共37500读出更新，编码器保持冻结；约4分13秒。核对84项可用文件指纹、来源、500条历史、epoch0选模和初始化；重算2454指标均值、2664配对指标/70596分组，240组旧逐窗口误差数组复现。234项二进制引用未附带，本地未重跑真实模型/PCA；AutoDL验证回放、512PCA精确复现及896数值往返记录通过。

共享任务中MLP优于ridge，与上一轮独立位置结论不同；仍未通过保留位置插值。sampled两种子均值：训练位置共享MLP相对独立ridge，原品种/跨品种误差+14.64%/+18.05%；保留位置+140.94%/+194.69%，且两个种子三个保留位置在两研究集均差于current28共享MLP。末轮保留位置更差，不能继续增加头训练轮数当作解决方案。信息仍可被独立头读出，不宣称编码完全丢失。

PCA512虽保留99.827%训练方差，原品种/跨品种综合误差仍0.040667/0.016723，一步相关性0.844/0.827；768降至约2.47e-12/1.06e-12。当前固定512PCA输出子空间确有重建上限，但神经模型在实体/量仓仍未达到同维PCA，学习利用不足也存在。768近完整重建部分可由目标冗余解释：EMA32偏移与log成交量差分有线性递推关系，理想完整通道下有效仿射空间上限约763；不是市场固有维度证明。

下一建议进入编码器训练：512/768容量路线×仅末端/随机多位置共享历史监督，两个种子，同分区/采样/窗口曝光/更新预算，保留全局重建并测试监督未覆盖的位置。宽度与PCA rank当前绑定，不能单独归因为embedding维度；不同时改层数/头数/特征。现有对照只在严格复现后复用，无自动晋升。本次只复盘，未实现新训练；详见[BABEL_SHARED_READOUT_REVIEW.md](BABEL_SHARED_READOUT_REVIEW.md)。

## 2026-09-23：逐bar表示的收敛矩阵已准备

用户批准继续并要求逐步收敛。实现[BABEL_BAR_ALIGNMENT.md](BABEL_BAR_ALIGNMENT.md)：只比较512/768 × 末端/多位置监督，两个种子共八格。所有格从头200轮，沿用同种子原窗口采样与批次顺序，每格7600次编码器和7600次共享头更新；不同时改变层数、头数、特征或预算。旧模型未训练在线局部头，不能直接作为同预算控制复用，因此八格全部重跑。仅宽度与PCA rank同时变化，不宣称纯维度效应。

每窗口每轮在32..128排除48/80/112后抽四位置，重建各位置当前bar之前16根历史，局部权重固定0.25。各格共享头都训练；endpoint切断局部损失对编码器的梯度，且头与编码器独立优化/裁剪。joint允许局部梯度进入编码器。两个优化器及RNG、绝对epoch、窗口和位置曝光均可审计恢复。验证只用32/64/96/128选择G+0.25L，包含epoch0；八格全部锁定后评价研究集，报告best/last与全部位置。

预先固定收敛门槛：相对本轮512_endpoint，保留位置局部误差改善至少10%，全局primary退化不超过5%，主要分项不超过10%，采用缩放参考的周配对区间，两个种子、两个研究集、best/last均须通过。优先512；768升级还需要相对较小合格方案至少5%的额外局部收益并保持全局边界。无合格新路线则保留基线、停止容量/监督搜索；通过后转状态接口用途验证，不自动扩模型、调权重或延长训练。门槛是探索性工程取舍，反复使用的研究集不冒称独立泛化。

相关本地检查74项通过，含9项新增：历史目标与旧缓存一致、当前/未来隔离、控制核心梯度精确一致、joint梯度、实际512/768维合成前后向、固定位置计划、双优化器恢复、锁定/篡改/预算检查、禁止神经优化器更新的八格完整流程及失败打包。旧训练源码未改。正式训练仅在AutoDL执行，当前没有新训练结果。每次运行自动向download导出完整成功/失败报告。


## 2026-09-23：逐bar收敛矩阵复核，停止容量扩展

收到bar_alignment报告，详见[BABEL_BAR_ALIGNMENT_REVIEW.md](BABEL_BAR_ALIGNMENT_REVIEW.md)。八格均200轮、编码器/头各7600步；48项当前可用指纹、1600条预算/位置采样历史、16份验证回放记录通过，重算2048均值、720配对指标/19080分组和最终决策。原512_endpoint两种子固定末轮在两研究集40个逐窗口全局指标数组精确复现旧sampling控制。归档无权重，本地未重跑真实推理/训练。

多位置监督的作用明确：512_joint保留位置局部误差相对512_endpoint下降93.23%/97.26%，768_joint下降94.39%/97.82%；单独加宽仅下降7.27%/7.97%。768_joint全局综合改善25.41%/28.34%，但seed43路径分项best原品种/跨品种增加13.91%/15.35%，完整验收仍失败。三条新路线均不合格，不以两个种子均值掩盖失败，不调整门槛。保留基线并结束本次容量/监督搜索。

中间位置改善不能代替最新状态完成：768_joint的第128根局部误差约0.398/0.296，保留位置约0.035/0.008。每窗口全局目标都监督h128，末端局部目标却只获得四位置均匀采样中的约4.3%曝光，其期望加权系数约0.25/94；全局坐标约束与局部目标的末端角色差异是待验证解释，不能直接宣称表示丢失。best靠近200且后期验证继续下降，不构成自动延训理由。

下一建议仅冻结权重、零更新诊断末端：同一个h128的共享局部头，与全局解码后重新锚定/截取的同一16根历史做同尺度对照，并核验训练后因果/窗口语义。先定位能否通过现有读出取回近期信息，再决定是否进入用途验证。本次仅复核并记录，不实现新训练，不再扩维/扩层/改EMA或搜索损失权重。

## 2026-09-23：冻结末端读出诊断已准备

用户同意后实现[BABEL_ENDPOINT_READOUT.md](BABEL_ENDPOINT_READOUT.md)。只读上一轮八格的best/last共16个现有检查点，0次编码器/读出头更新、0次统计拟合。同一h128经原局部头和原全局PCA解码器比较；全局结果裁取第112–127根，并以自己的预测第111根重新锚定，禁止使用真实锚点校正。复现旧验证及两研究集的全局、p128逐窗口误差，保留两个种子和best/last、分项/通道/一步诊断、周配对和固定样例。

对每个已训练检查点使用固定验证窗口核验严格前缀、篡改/追加未来、独立调用和批次独立性。FP32失败保留误差并同权重FP64复查，明确标记数值漂移或因果失败；128上下文与重置64上下文只报告差异，不冒称流式等价。全部源选模先锁定，只读核验来源和权重，按检查点恢复完成的诊断；旧训练源码不改。

83项相关本地检查通过，含9项新增，覆盖预测锚点/当前排除、真实512/768维合成推理、故意泄漏的拒绝、数值复查分支、零优化器/零拟合完整16检查点流程、来源不变、断点跳过、篡改拒绝、失败诊断和打包。尚未在AutoDL运行真实权重，未预写结论。本轮输出保留原失败晋升决策，不以读出改善自动替换模型，也不触发新的训练矩阵。成功或失败统一打包到download。

## 2026-09-23：冻结末端读出已复核，收束诊断并准备接口交付

收到endpoint_readout报告，详见[BABEL_ENDPOINT_READOUT_REVIEW.md](BABEL_ENDPOINT_READOUT_REVIEW.md)。16个已有检查点完整，模型/头更新与拟合均0，运行约1分42秒。核对当前152及来源53项可用hash，重算384均值、192配对指标/5088分组、192个原p128逐窗口数组；64个best样例通过上一轮全局预测独立重算裁切，确认使用模型自身锚点。560项已训练检查点的因果/独立调用检查均通过原FP32容差，未触发FP64复查。归档无权重，本地未重跑真实模型。

同一h128经全局解码后裁取近期16根，在全部32组检查点/研究集组合及六个分项均有配对改善。两种子best均值：512_endpoint局部综合下降49.36%/52.31%；768_joint下降31.74%/38.22%，后者收盘MAE降至16.86/11.75bp，一步相关性约0.743/0.731、幅度比0.735/0.746。证明部分近期信息可从向量读出，不能将原局部头失败当作信息全部丢失；剩余误差及原全局路径保留失败仍然存在，不能晋升768。

结束容量和读出诊断分支，不启动新的训练矩阵。保留512_endpoint基线、原失败决策和两种子研究候选。下一建议为固定窗口状态接口：返回来源/结束时间、h128、全局与自预测锚点近期重建；需要逐bar输出时以截至各bar的滚动128窗口末端定义状态，遵循合约和因果预热边界，不把单次窗口的异质中间状态冒称统一接口。原全历史/通用表示目标尚未完成，接口产物不作预测或交易能力承诺。本次仅复核，接口尚未实现。

## 2026-09-23：固定窗口状态接口交付已准备

用户批准后实现[BABEL_WINDOW_STATE.md](BABEL_WINDOW_STATE.md)。保留原512_endpoint两种子best检查点及原候选失败决策；不训练、不拟合统计、不重新选择种子。新增模块打包核心权重与固定统计，移除对旧checkpoint目录的运行依赖；运行仍需匹配源码及依赖。旧训练源码未改。

每个已收盘bar用滚动128根的末端表示定义状态，返回512维向量、前127根全局重建及以预测锚点裁取的近期16根。EMA/波动和成交持仓因果特征沿单合约持续更新，512根预热后输出；换合约显式重置，不自动主力切换。支持as_of时间边界、快照恢复、仅预热历史和逐bar输出。不将原窗口中间状态当作等价末端，也不把当前bar输出为已验证重建目标。

正式交付只读复现两种子两研究集旧全局/近期逐窗口误差，固定四合约各九个连续状态检验28通道增量特征、原缓存、末端表示、追加未来不改前缀与恢复一致性。全部通过后才签发模型包可加载凭证；失败撤销本次凭证且导出诊断。报告不含权重，另行成功导出含固定权重的bundle到download。这里数值回放不构成全市场逐bar质量验证，更不新增预测或趋势标签结论。

52项相关本地检查通过，含9项新增，覆盖独立批量/增量特征比较、已收盘与预热边界、自预测锚点、精确恢复、异常事务性、CSV追加续跑、无优化器/无拟合旧指标回放、打包篡改拒绝、交付失败撤证和两类导出。全部使用合成数据及原测试夹具；正式真实权重回放仍待AutoDL，不预写成功结论。下一阶段基于冻结接口验证明确用途，保持主线收敛，不另开容量和损失矩阵。
