# control600固定研究接口：打包与回放通过

2026-09-26。来源`babel_control600_v2_reports.tar.gz`，SHA256：`8b7121039ff70d9e0b8d0a389ab8ef6f942008d760315c4aaa4c45aa57d8a3c0`。完整机器可读核验记录见[BABEL_CONTROL600_REVIEW.json](BABEL_CONTROL600_REVIEW.json)。

## 结论

本轮工程收尾：control600两种子原best600模型、原局部头和原校准读出已成功打包，原结果回放及固定滚动接口检查通过。耗时263.52秒（约4分24秒），命令退出0；编码器更新、头更新、读出拟合、标准化拟合均0。

原PathInput结构恢复问题已经通过本次真实权重打包路径验证。保留严格加载、原模型签名及来源不变检查，没有删除权重字段、放宽阈值或改变路径开关。模型包的加载凭证指向正确的index，index对应正确的交付manifest与两种子权重摘要。

这是原有能力的可靠封装，不是一次新增学习收益；不改变原完整用途未通过、512既有交付基线保留及研究数据反复使用的结论。

## 核验范围与证据

- completion绑定27个产物，本地核验其中25个报告/索引文件哈希；2个`.pt`按报告导出规则省略。62份交付源码和59份运行包源码哈希与仓库一致。
- 原跨周期补评价manifest、全部绑定报告及completion与已复核来源一致。校验凭证、索引、模型文件摘要和样例中的身份链一致。
- 2种子×2研究集（原品种984、跨品种453窗口）的全部8份重建/用途回放诊断通过、差异列表为空。对源报告独立比较评分和16个等距样例，共29576个数值叶子通过原容差。
- 4个固定合约×2种子×9个连续滚动端点共72次检查均通过；状态对独立batch1参考最大差异0。增量特征、原缓存、未来后缀特征前缀及快照恢复均通过。
- 对导出的8份末端状态，独立核验768维、127/16历史长度、当前bar排除、时间可用性、来源、价格还原公式及模型自身近期锚点。近期绝对价格与全局对应裁切一致。
- 8份状态的13个读出对原研究缓存对应行最大差异8.7641e-6，在既有同窗口FP32批次控制5e-5/2e-4内；不是放宽原batch64研究评分回放门槛。
- 离线HTML独立从原已复核报告重建，文件哈希与本次导出完全相同，没有替换样本或修改展示口径。

报告没有模型权重，以上本地检查没有重新执行真实模型或训练。72次固定连续状态验证不能推广为所有滚动bar质量已验收；其意义是接口数值与状态管理一致性。

## 现有效果：两个种子的均值

| 指标 | 原品种研究集 | 跨品种研究集 |
|---|---:|---:|
| 全局127根收盘路径MAE（log基点） | 11.83 | 7.09 |
| 近期16根、自身锚点路径MAE（log基点） | 9.38 | 6.13 |
| 近期单步变化相关性 | 0.9355 | 0.9339 |
| 近期单步变化标准差比（重建/真实） | 0.8924 | 0.9069 |

这些数值来自原模型的现有研究结果。本轮原样复现，没有新增对照或独立泛化试验；相关性不是预测准确率。幅度比仍小于1，说明总体变化幅度偏小，不能宣称细节无损。跨品种的绝对误差较低也不等于跨品种泛化更强，行情分布及波动水平不同。

## 收束与下一阶段

用户不需要重新训练、继续打包或补交权重。本次研究模型包与离线图保留即可，接口已可用于明确标注的历史状态研究。

下一阶段回到核心表示问题，依据[BABEL_NEXT_CORE_REASONING.md](BABEL_NEXT_CORE_REASONING.md)完整论证统一bar中心历史表达/共享解码与联合训练。原末端PCA坐标、不同长度中途上下文和独立局部头之间的职责需重新审视；这是假设，不是本次接口验证得出的因果结论。允许较大结构调整，但保留冻结原模型、同预算原续训和冻结编码器新读出对照，事先确定价格坐标、主要增益及原能力保护，禁止只靠更好看的曲线宣布突破。尚未锁定或启动新训练矩阵。
